# 새 룰북(Core Rules 2026-07-16) 대조 수정 내역 — 2026-09-11
시뮬레이터가 따르던 2025-12-01판 이후 Unleashed(2026-03-31)·Vendetta(2026-07-16) 개정을 반영. 룰북 7,321행을 5개 영역(게임 개념·턴·체인·결전 / 플레이·대상·격발 / 이동·전투·득점·층 / 액션 / 키워드)으로 나눠 전수 대조.
발견 52건 (확인됨 25 · 유력 13 · 불확실 14 · 알려진 절충 14) → 수정 대상 25건 중 수정 23 · 부분 0 · 보류 2

## 수정 내역
### 룰 190.6 / 318 step 4 (work-A no.1) — 전장 통제 상실 시점 — 닫힌 상태에서는 유지
- ✅ 수정 — 기존: releaseEmptyBattlefields가 모든 cleanup에서 유닛 없는 통제 전장을 즉시 무주공산으로 만들고 숨김 카드를 폐기했다(결전 체인 항목 하나가 해결될 때마다, 중립 응수 창 안의 반응 카드 해결 직후에도).
  → 변경: chainClosed()(결전 체인에 항목이 남았거나 중립 응수 창 안 = G._rwFor)이면 releaseEmptyBattlefields를 건너뛰고, 체인이 빈 열린 상태의 클린업(마지막 체인 항목 해결 뒤 · 원 주문 해결 뒤)에서만 통제 해제·숨김 카드 폐기. reactionWindow의 [반응] 능력 발동 경로에도 G._rwFor를 표시. 봇 샌드박스(bot-policy POLICY.reaction의 finish)는 응수 창이 끝난 것으로 보고 G._rwFor를 지운 뒤 해결·클린업.

### 룰 316.9.b.1 / 318 step 10a / 460.1 / 464 Step 1 (work-A no.2 · work-C no.9) — 비전투 결전 → 전투 결전 승격 시점
- ✅ 수정 — 기존: 무혈 결전 중 상대 유닛이 들어와도 결전이 그대로 진행되고, 빈 체인에서 양측이 패스해 결전이 끝나는 시점(resolveShowdown)에 G.showdown을 새로 만들어 '새 전투 결전'을 열었다(지정·[방어 시]/[공격 시]·전장 [방어 시]는 그때, 포커스는 공격자부터·패스 라운드 하나 추가).
  → 변경: cleanup()이 사망 처리·통제 해제 뒤에 'G.showdown && !hasCombat && 체인 비어 있음(결전 열린 상태) && 양측 유닛 존재'면 같은 결전 객체를 그 자리에서 전투 결전으로 승격(openCombat: 공격자=경합 적용자, 지정·가면 스냅샷 → 전장 [방어 시] → 지정 격발 → 격발 피해의 치명 판정). 포커스·체인·패스 카운터는 손대지 않는다(체인이 닫히는 규칙(346)이 그대로 적용). startShowdown도 같은 openCombat을 쓴다. resolveShowdown의 옛 재개 경로는 폴백(클린업 없이 유닛이 들어온 경우)으로만 남기되 결전을 닫지 않고 승격해 잇는다. G.showdown 필드를 읽는 ui.js/bot-policy/bot-eval/bot-sim은 hasCombat·attacker·defender를 매 판단마다 새로 읽으므로 변경 불필요.

### 룰 466.3 Resolution Step Reminder (work-C no.11) — 전투 사망 격발([죽음의 종소리]·사망 이벤트)의 해결 시점
- ✅ 수정 — 기존: resolveShowdown이 보류한 전투 사망 격발을 전투 정리(치유·귀환) → 통제 확립 → 정복 득점·정복 격발까지 끝난 뒤에 해결했다. 공격자 전멸·방어자 1기 잔존·공격자 종소리가 그 방어자를 처치하는 경우 방어자가 먼저 통제 확립·정복 득점한 뒤 죽었다.
  → 변경: 전투 정리(치유·공격자 귀환) 직후, 승패 판정 전에 보류 격발을 해결하고 cleanupDeaths로 치명 판정까지 마친 뒤 잔존 유닛을 다시 센다 — 위 예는 '유닛 없음'으로 통제 확립·정복 없음. 양측이 남으면(격발이 유닛을 옮겨 온 경우) 결과 없음으로 경합을 유지해 마지막 cleanup이 새 결전·전투를 연다(466.5.d). 기존 test-batch3 '종소리는 치유 뒤' 기대는 그대로 성립.

### 룰 466.7 / 467 Combat ends (work-C no.19) — 공격/방어 지정 해제·'이번 전투' 효과 만료 시점
- ✅ 수정 — 기존: 통제 확립·정복 격발보다 먼저 G.showdown=null(지정 소멸)·G.state='neutral'로 만들고 '이번 전투' 부여 키워드(_combatGrants)도 치유 직후에 되돌렸다 — 정복 격발 시점에 [맹공]이 안 붙고 where:'combat' 대상이 비었다.
  → 변경: 통제 확립·정복 판정·정복 격발 동안 G.showdown(sd)과 G.state='showdown'을 유지하고, 정복 격발이 끝난 뒤 '전투 종료' 단계에서 _combatGrants 되돌리기 → G.showdown=null → G.state='neutral' → 마지막 cleanup 순으로 처리. 정복 격발 중 cleanup이 불려도 결전 상태라 새 결전을 열지 않고(마지막 cleanup이 연다), 결전 전장은 releaseEmptyBattlefields가 건너뛴다.

### 룰 323.11 / 190 (work-C no.24) — 빈 전장의 통제 상실 시점 (체인에 항목이 있으면 유지) — no.1과 동일 수정
- ✅ 수정 — 기존: 결전 체인의 항목 하나가 해결될 때마다 불리는 cleanup에서 다른 빈 전장의 통제가 풀렸다(체인 아래 항목이 '내가 통제하는 전장'을 참조하면 신판과 다름).
  → 변경: no.1의 chainClosed() 가드로 결전 체인이 빈 클린업에서만 해제. 헤드리스 테스트: 체인 [A 207, B 133]에서 133이 A의 1번 전장 유일 유닛을 죽여도 207이 해결될 때까지 통제·숨김 카드 유지, 그 뒤 무주공산·숨김 폐기.

### 룰 341 / 318 step 9 (work-A no.25) — 중립 닫힌 상태(응수 체인 중) 결전 개시 차단
- ✅ 수정 — 기존: cleanup()이 G.state!=='neutral' || G._holdShowdown만 검사해, 중립 응수 창 안에서 반응 카드가 해결된 뒤의 cleanup이 경합 전장을 발견하면 원 주문이 미해결인데도 startShowdown이 실행될 수 있었다.
  → 변경: cleanup의 결전 개시 조건에 G._rwFor==null(중립 응수 창 밖 = 중립 열린 상태)을 추가. 원 주문이 해결된 뒤의 cleanup이 결전을 연다. 봇 샌드박스의 finish는 G._rwFor를 지워 시뮬레이션에서 결전이 정상 개시.

### 룰 338 Step 1~2 (work-A no.14) — 중립 상태 체인에서 시전자의 우선권 유지(자기 주문 위에 먼저 [반응] 적재)
- ⏸ 보류 — 기존: 중립 상태에서 주문을 내면 곧바로 상대의 응수 창(reactionWindow)이 열리고, 시전자는 상대가 반응한 뒤의 재응수 창(_returnPending)에서만 낼 수 있다.
  → 변경: 변경 없음.
  _보류 사유: 룰북(338 'Finalizing an item to the chain does not pass Priority')은 뒷받침하지만 감사 영향도 low·실전 빈도 낮음. 구현하려면 모든 중립 주문 플레이 앞에 시전자용 '자기 주문 위 [반응] 적재/패스' 창(UI.pickReaction)을 열어야 해 사람 플레이어에겐 [반응] 카드를 든 채 주문을 낼 때마다 모달이 하나 추가되고(UX 비용), 온라인 락스텝·봇 POLICY.reaction(카운터 옵션·pendingSpell 시뮬 분기)·기존 헤드리스 테스트(REACT 콜백이 p를 보지 않고 부작용을 실행: test-batch7 ②·③)에 모두 손대야 한다. 위험 대비 이득이 작아 의도된 절충으로 남긴다 — 시전자는 상대 반응 뒤 재응수 창에서 낼 수 있다._

### 룰 460.1 / 462.2.b / 464 Step 1 (work-C no.9) — 결전 진행 중 전투가 준비되면 클린업에서 전투 결전으로 승격 (포커스 유지) — no.2와 동일 수정
- ✅ 수정 — 기존: cleanup()이 결전 중에는 즉시 반환해 전투를 열지 않고, 결전 종료 시 startShowdown(…, true)로 새 전투 결전을 열며 포커스를 공격자에게 새로 주고 패스 카운터를 0부터 다시 시작했다.
  → 변경: no.2의 cleanup 승격 경로(openCombat)로 같은 결전이 이어진다 — G.actingPlayer(포커스)·passes는 승격이 건드리지 않고 체인 닫힘 규칙(346)대로 넘어가며 추가 패스 라운드가 생기지 않는다. 헤드리스 테스트: B의 「바람 타기」 해결 뒤 G.showdown 객체 동일·hasCombat=true·야스오 [공격 시] 즉시·「요새화된 진지」 [방어 시] 격발·포커스 A·passes 0.
  _보류 사유:  감사 영향도 low·실전 빈도 낮음. 구현하려면 모든 중립 주문 플레이 앞에 시전자용 '자기 주문 위 [반응] 적재/패스' 창(UI.pickReaction)을 열어야 해 사람 플레이어에겐 [반응] 카드를 든 채 주문을 낼 때마다 모달이 하나 추가되고(UX 비용), 온라인 락스텝·봇 POLICY.reaction(카운터 옵션·pendingSpell 시뮬 분기)·기존 헤드리스 테스트(REACT 콜백이 p를 보지 않고 부작용을 실행: test-batch7 ②·③)에 모두 손대야 한다. 위험 대비 이득이 작아 의도된 절충으로 남긴다 — 시전자는 상대 반응 뒤 재응수 창에서 낼 수 있다._

### 룰 356.4.c.1 / 356.4.d (work-B no.5) — '최소 [1]' 할인의 최소값은 그 할인에만
- ✅ 수정 — 기존: applyCostMods가 모든 할인을 합산한 뒤 최종값에 min(minE)을 한 번 적용해, 견습생(84)이 전장에 있으면 「하늘 가르기」·'다음 주문 -5'(격노한 화염룡) 등 다른 할인도 1 밑으로 못 내려갔다.
  → 변경: 최소값이 있는 할인(견습생 spellDiscount 최소 1 · 비늘의 전령 tagDiscount min)을 먼저 그 최소값까지만 깎고(이미 그 밑이면 올리지 않음), 최소값 없는 할인(자기 할인·nextSpellDisc)은 그 뒤 0까지 이어 깎는다 — 룰북 예시(견습생 + 하늘 가르기 8 → 7 → 0)와 일치. 플레이어에게 유리한 순서가 항상 나온다(순서 선택 허용).

### 룰 377.3.b.2 / 403.1.b / 404 (work-B no.6) — 활성화 능력의 대상 지정 시점·발동 적법성·응수 창
- ✅ 수정 — 기존: ab.target(무허가 무기고 23)·ab.preTarget(미끼 바늘 242·미래의 용광로 212)이 있는 3장만 발동 시점에 대상을 고르고 후보가 없으면 막았다. 그 외 능력(강철 발리스타 17·케이틀린 68·후회의 보주 90·투기장 주점 124·리 신 257·미스 포츈 267)은 후보가 없어도 탈진·비용을 낸 뒤 해결에서 불발했고 대상은 해결 시점에 골랐다. 중립 상태 응수 창은 preTarget이 있는 비전설 능력에만 열렸다.
  → 변경: preTargetAbility(주문의 preTargetSpell과 같은 preTargetOps 공용 코어)가 능력 ops의 대상 spec을 발동 시점(비용 지불 전)에 고정해 ctx.pre로 넘기고, 필수 대상 후보가 없으면 발동 거부(404). abilityHasTargets를 봇 polAbLegal이 같이 써서 봇도 같은 판단. 중립 상태에서는 [추가] 자원 능력(isResourceAbility)만 빼고 모든 활성화 능력(전설 포함)에 reactionWindow(ability:true)를 연다 — 응수로 대상이 사라지면 그 지시만 불발(356.3.e). 결전 중 체인 적재 경로도 pre를 들고 간다. 굴절(810)은 능력 비용과 합산해 후보 판정.

### 룰 420.3.a / 420.3.b (work-B no.8) — 'played this turn' 비격발 판정 = 파이널라이즈 기준, 격발은 해결 기준
- ✅ 수정 — 기존: 주문의 플레이 수(P.playedCards)는 해결 시점(fireSpellPlayEvents)에서만 +1 — 체인에 적재만 된 주문·카운터당한 주문은 세지 않아 [군단]·군단 할인·군단 활성화 능력이 미충족이 됐다. 다리우스 '두 번째 카드'(seq)도 같은 값을 봤다.
  → 변경: '파이널라이즈 카운트'와 '격발용 순번'을 분리: P.playedCards는 비용 지불·손패 이탈 직후(체인 적재·카운터 여부 무관, 중립 응수 창의 카운터 주문 경로 포함)에 +1 — 군단·할인·활성화 능력·polAbLegal이 보는 값. 새 P.playedSeq는 해결 시점(fireSpellPlayEvents·유닛/도구 플레이 직후)에만 +1 하며 onYouPlayCard 등 격발 이벤트의 seq로 쓴다(카운터당한 주문은 격발하지 않고 순번도 안 늘어남). 탈취된 주문의 파이널라이즈 수는 원 시전자 몫, 격발 순번은 해결 시 통제자(탈취자) 몫. 기존 테스트 기대값 갱신: test-batch1 ②('카운터된 주문 플레이 수 0'→1)·탈취(B=2,A=0 → A=1,B=1·seq B=2), test-batch5 '#9459'(0→1)·'저항 자기 광선'(1→2), 다리우스 시나리오는 playedSeq=1도 세팅.

### 룰 358 / 359.2.d (work-B no.31) — 활성화 능력 비용 지불 중 취소 시 되돌림
- ✅ 수정 — 기존: activateAbility가 탈진·에너지/힘을 먼저 낸 뒤 cost.spendBuff가 없으면 return, cost.killFriendlyOrGear 선택을 취소하면 return — 이미 낸 탈진·자원이 환불되지 않았다.
  → 변경: spendBuff 부족 검사를 지불 전(canPay 옆)으로 올리고, killFriendlyOrGear의 처치 대상 선택을 지불 전에 받아 취소하면 아무 비용도 내지 않은 상태로 끝난다(357.2 비표준 비용은 임의 순서). 실제 처치는 탈진·자원 지불 뒤에 수행.

### 룰 383.4.b ~ 383.4.c (work-B no.32) — 꿈꾸는 나무(292) '주문으로 이곳의 아군 유닛을 고를 때' 격발 시점
- ✅ 수정 — 기존: pickPreTarget이 대상을 클릭한 직후·굴절 지불 전에 noteSpellPick으로 드로우해, 굴절 거부로 재선택하거나 PRE_CANCEL로 플레이가 취소돼도 이미 드로우했고 '턴마다 처음' 플래그(TF().bf292)도 소모됐다.
  → 변경: preTargetSpell이 고른 유닛 목록(pre._units)을 돌려주고, playCardFromHand가 비용 지불·손패 이탈(파이널라이즈) 직후에 noteSpellPick(p,u,'spell')을 호출한다. 취소된 플레이는 격발하지 않는다. 해결 시점에 UI.pickUnitFrom을 직접 쓰는 커스텀 op의 noteSpellPick 호출은 그대로.

### 룰 812.1.c / 812.2 / 813.1 (work-D no.11) — 군단 — 조건 판정 기준이 '해결'이 아니라 '파이널라이즈'
- ✅ 수정 — 기존: 군단 조건(P.playedCards>=1)이 해결된 주문만 셌다 — ① 카운터당한 주문 뒤의 군단 카드 미충족 ② 결전 중 체인에 미해결 주문이 있는 동안 낸 군단 카드 미충족 ③ 군단 주문은 플레이 시점 스냅샷(legionOK)만 사용.
  → 변경: no.8의 파이널라이즈 카운트로 ①②가 해소되고, ③은 resolveChainItem이 해결 시점에 legionOK = 스냅샷 || (자기 자신 포함 P.playedCards>=2)로 재평가한다(종속 능력은 조건이 참인 동안 활성 — 813.1). 군단 활성화 능력·군단 할인·격발의 군단도 같은 값을 본다. 유닛/도구 등장 격발의 ctx.legionOK 스냅샷은 등장 직후라 그대로.

### 룰 810.1.c.3 — 카드 원문 예외 (work-D no.26) — 굴절 합산 vs '정령의 안식처'(63) "if they didn't already"
- ✅ 수정 — 기존: 정령의 안식처의 kwAura deflect가 addKw로 무조건 +1 합산되어, 인쇄 [굴절] 유닛(죽음꽃 포식자 161 등)이 버프를 받으면 굴절 2가 됐다.
  → 변경: kwAura에 ifNotHas 플래그를 두고 effKw가 base에 이미 그 키워드가 있으면(인쇄·부여·앞선 오라) 건너뛴다. 63의 statics에 ifNotHas:true. 다른 합산 오라(타릭 [보호막] 등)는 영향 없음.

### 룰 817.2 / 817.2.a (work-D no.27) — 통찰 — 인쇄 [통찰] + 오라 [통찰]은 각각 격발
- ✅ 수정 — 기존: 유닛 플레이·토큰 플레이 시 `fx.kw.vision || effKw(u).vision`으로 [통찰]을 한 번만 예측했다.
  → 변경: visionCount(u) = 인쇄 1 + 부여 1 + 매칭되는 kwAura 출처 수(예언자 100 2기면 2)만큼 visionCheck를 반복한다(인스턴스마다 재활용 여부를 따로 선택, 재활용하지 않으면 같은 카드를 다시 본다 — 817.2.b). 도구의 인쇄 [통찰]은 1회 그대로(오라는 유닛 대상).

### 룰 717.3 (work-D no.38) — 보너스 피해 — 피해 0 인스턴스에는 붙지 않음
- ✅ 수정 — 기존: dealDamage가 「레이븐본 서적」 nextSpellBonus를 n<=0 판정보다 먼저 더했고(RiftJudge #5791 근거), 범용 'damage' op·카드 스크립트(쌍권총 난사 268·티모 121·황혼 광선 등)도 기본 피해와 무관하게 effDmgBonus(관문·애니)를 더했다.
  → 변경: dealDamage는 기본 n<=0이면 보너스 가산 전에 0 반환. 새 헬퍼 dmgPlus(n,u,p)=n>0?n+effDmgBonus:0으로 engine의 damage/damageAll·cardscripts의 dmgEqMyMight·dmgLastDiscardCost·티모 hiddenCnt·gunsBlazing·기타 op.n 기반 호출을 교체(상수 피해 2·6 등 항상 양수인 호출은 그대로). test-batch7 ⑬ 기대값(힘 0 → 관문 +1)을 0으로 갱신(717.3이 구판 #5791을 뒤집음).

### 룰 431.3 / 431.3.c / 431.4 (work-E no.9) — 번아웃 반복 (덱·폐기장 모두 빈 경우)
- ✅ 수정 — 기존: drawCard가 덱이 비면 burnOut을 1회 호출한 뒤 deck.shift()가 undefined이면 끝 — 폐기장까지 비어 있으면 드로우 시도 1회당 상대 1점만 주고 드로우는 미완.
  → 변경: ensureDeck(p): 덱이 빈 동안 승자가 나올 때까지(가드 64회) burnOut을 반복 — 매번 상대 1점. drawCard와 「맹목적 분노」(no.37)가 공용. 431.4.b(두 번째 이후 번아웃 점수로 즉시 승리)는 addPoints→checkWin이 즉시라 일치.

### 룰 428.6 / 428.1.a (work-E no.12) — 'Kill all …' 복수 처치의 동시성
- ✅ 수정 — 기존: execOps 'killAll'이 `for(const u of us) await killUnit(u)`로 한 마리씩 죽여 첫 유닛의 종소리가 둘째 유닛이 죽기 전에 해결되고, 존야의 모래시계도 통제자 선택 없이 배열 앞 유닛부터 구했다.
  → 변경: `await killUnitsTogether(us)` — eachPlayerKills·cleanupDeaths와 같은 배치 경로(존야 대상은 통제자 선택, _dyingBatch·카서스 종소리 스냅샷). 파서의 'Kill each/all …' 및 파괴 공작원(159) 공격 격발이 해당.

### 룰 416.3.a = 신판 417.1 (work-E no.17) — 동시 재활용은 덱 밑에 무작위 순서
- ✅ 수정 — 기존: 「신성한 심판」(244) divineJudgment는 유닛·도구·손패 나머지를 보드 순·손패 순으로, 「미래의 용광로」(212) pickRecycleTrashes는 고른 순서로, 「촛불 밝힌 성소」(291) scryTop은 물어본 순서로 deck.push 했다.
  → 변경: 세 곳 모두 재활용할 카드를 플레이어별로 모은 뒤 `deck.push(...shuffle(list))`. 룬 덱 재활용은 no.34(보류) 그대로.

### 룰 414.1 (work-E no.33) — Exhaust 'a unit'의 대상 진영
- ✅ 수정 — 기존: execOps 'exhaust'가 spec.side 'any'를 'enemy'로 강제해 아군 유닛을 탈진시킬 수 없었다.
  → 변경: spec을 그대로 사용(아군도 선택 가능). 봇은 프롬프트('탈진시킬')가 POL_HARM에 걸려 적 최강을 고르는 기존 정책 유지.

### 룰 416.3.b = 신판 417.2 (work-E no.34) — 룬 덱 동시 재활용 순서는 소유자 선택
- ⏸ 보류 — 기존: payCost가 힘 지불로 룬 여러 개를 재활용할 때 처리 순서대로 runeDeck.push 하고, 「신성한 심판」의 룬 재활용도 보드 순서 고정.
  → 변경: 변경 없음.
  _보류 사유: 룰북(417.2)은 뒷받침하지만, 힘 2핍 이상을 룬 재활용으로 내는 모든 지불마다 순서 선택 모달(pickOption)이 추가되어 사람 플레이어의 UX 비용이 크고, 봇·온라인 락스텝·기존 헤드리스 테스트(OPTLIST 순서를 보는 것들)에 프롬프트가 늘어나는 데 비해 효용(다음 전개 단계에 어느 속성 룬이 먼저 나오는가)은 작다. 감사 메모도 '보류해도 되는 항목'으로 분류 — 의도된 절충으로 남긴다._

### 룰 416.6 (work-E no.35) — 효과 재활용은 강제 (가능한 한 많이)
- ✅ 수정 — 기존: recycleFromTrash(「문도 박사 - 전문가」 등 'recycle 3 from your trash')가 pickOption의 취소(null)에서 break — 남은 장수를 재활용하지 않고 끝낼 수 있었다.
  → 변경: 취소 시 폐기장 마지막 카드를 자동 선택해 지정 장수만큼(폐기장이 허락하는 한) 반드시 재활용 — 비용 recycleTrash와 같은 폴백.

### 룰 423.1 (work-E no.36) — Stun 'a unit'의 대상 진영
- ✅ 수정 — 기존: execOps 'stun'이 spec.side 'any'를 'enemy'로 바꿔 적 유닛만 고르게 했다. 플레이 시점 지정(preTargetSpell은 원래 spec any)에서 아군을 고르면 해결 때 enemy spec에 걸려 '더 이상 유효하지 않음'으로 불발되는 불일치도 있었다.
  → 변경: op.spec을 그대로 사용 — 아군도 고를 수 있고 플레이 시점 지정과 해결 시점 spec이 일치. 봇은 프롬프트('기절')가 POL_HARM에 걸려 적을 고르는 정책 유지.

### 룰 431.1.b (work-E no.37) — 덱 맨 위 카드를 다른 존으로 옮기는 효과(「맹목적 분노」 25)의 번아웃
- ✅ 수정 — 기존: blindRage가 `if(!O.deck.length) return;`으로 상대 덱이 비면 아무 일 없이 끝났다.
  → 변경: 추방 전에 ensureDeck(o) — 상대가 번아웃(폐기장→덱, 시전자 1점)한 뒤 나머지(공개·추방·플레이)를 완료하고, 폐기장도 비면 431.3대로 반복 번아웃.

## 수정하지 않은 발견 (불확실 · 알려진 절충)
- [B] 룰 355.11 ~ 355.11.b (집단 대상 요건, Fox-Fire) 여우불(256) '총 위력 4 이하 원하는 수만큼 처치'의 대상 지정 시점 (confirmed · 절충) — 알려진 절충 ④(다중 대상은 해결 시 선택)의 연장. 정식 구현은 PRE_TARGET_EXTRA.foxfire로 플레이 시 집단을 고정(uid 배열)하고, 해결 시 원래 대상 중 합이 4 이하가 되는 부분집합을 시전자가 고르게 한다. 대상이 0기여도 플레이 가능(355.13).
- [B] 룰 355.14.e ~ 355.14.i (Splitting) 분할 피해의 대상 지정 시점·해결 시 대상 제외 (confirmed · 절충) — 알려진 절충 ④. 신판에서 '해결 시 대상 제외' 규칙(355.14.h~i)이 추가됐다. 정식 구현 시 플레이 때 대상 집합(≤초기 피해량)을 고정하고, 해결 시 피해량<대상 수면 시전자가 제외할 대상을 고르되 남는 대상 수는 피해량 이상이어야 한다.
- [B] 룰 383.3.b / 383.3.b.1 / 383.4.a / 402.x / 403.1 / 404.2 격발 능력의 체인 적재·파이널라이즈 시 선택/비용·순서·응수 (confirmed · 절충) — 알려진 절충 ①(격발 즉시 해결). 신판은 you may 선택·지시 내 비용을 '파이널라이즈 시'로 옮겼는데, 즉시 해결 모델에서는 '해결 시 선택'과 관측상 거의 같다(비용을 내는 순간과 효과 사이에 응수 창이 없다는 점만 다름). 진짜 차이는 ① 격발에 응수 불가 ② 동시 격발 순서
- [A] 룰 303.2.a / 305 (L1670-1672) 동시 격발의 플레이어 간 처리 순서 (confirmed · 절충) — 절충 ①(격발 즉시 해결)의 부속 문제. 규칙대로면 턴 플레이어의 격발이 먼저 체인에 올라가 LIFO로 '턴 플레이어가 아닌 쪽'의 격발이 먼저 해결된다(전투 지정 격발에서 방어자 먼저 처리하는 것과 같은 원리). 예: 유닛 사망 시 양측의 '유닛이 죽을 때' 격발(메아리의 망령·빅토르
- [A] 룰 318 step 9~10 (L1956-1961) 복수 전장 동시 경합 시 개시 순서 (confirmed · 절충) — 절충 ②. 신판에서도 턴 플레이어 선택은 그대로이며, 9단계가 10단계보다 먼저라 비전투 결전이 걸린 전장과 전투가 걸린 전장이 동시에 있으면 9단계에서 상태가 결전으로 바뀌어 비전투 결전이 먼저 열린다(전투는 그 결전이 끝난 뒤 클린업에서). 인덱스 순 고정을 유지하더라도 '비전투 결
- [E] 룰 428.1.a.1.a Kill (종소리 체인 적재) (confirmed · 절충) — 알려진 절충 ①(격발 능력 즉시 해결). 신판은 처치 지시 시 종소리를 먼저 체인의 Pending Item으로 올리고 마지막 상태를 기록하라고 명시(L4413-4425)하며, Vendetta는 'When I die' 전부에 마지막 상태 정보를 허용 — 정보 보존 부분은 이미 일치하므로 
- [C] 룰 456 / 352.7 (효과 이동의 목적지) 주문 이동의 목적지 선택 시점 (confirmed · 절충) — 알려진 절충 ④.
- [C] 룰 461.2 복수 전장 동시 경합 시 순서 (confirmed · 절충) — 알려진 절충 ②. 1v1 2전장에서 양쪽 다 경합인 경우에만 해당.
- [C] 룰 465.2.c.4 (Combat Showdown Step 4) 지정 격발의 체인 적재·응수 (confirmed · 절충) — 알려진 절충 ①. 유닛이 나중에 들어왔을 때 '다음 클린업에서 지정'(464.2.f) 대신 이동 즉시 지정·격발하는 것(fireAttackTriggers 261~266)도 같은 절충의 일부.
- [C] 룰 465.2.c.6~7 (Backline/Tank 배타적 요구) [탱커]를 받은 '마지막 배분'(Backline) 유닛의 배분 순서 (confirmed · 절충) — 알려진 절충 ③. Backline을 부여하는 Origins 효과는 없어 키워드화 자체는 실효 없음. 정식 구현 시 candidates에 '탱커+Backline' 유닛을 첫 순위 후보에도 포함해 배분자가 고르게 하면 된다.
- [D] 룰 717.2 분할 피해 — 보너스는 총량에 1회, 대상은 플레이 시점 지정 (confirmed · 절충) — 알려진 절충 ④(분할 피해 대상은 해결 시점). 규칙은 분할 대상을 'Targets'로 보아 플레이 시점에 고르고(굴절도 그때), 해결 시 피해량이 대상 수보다 줄면 통제자가 제외할 대상을 고른다(Vendetta). 보너스 합산 자체는 일치.
- [D] 룰 808.1.b.2 / 808.1.b.3 / 740.4.a.2.a 죽음의 종소리·격발 능력의 체인 적재(응수 가능)와 격발 비용의 파이널라이즈 시점 지불 (confirmed · 절충) — 알려진 절충 ①(격발 능력 즉시 해결). 신판은 'When I die' 전부가 종소리와 같이 죽기 직전 정보를 쓰도록 통일했고(엔진은 이미 그렇게 함), 격발 첫 부분의 비용은 파이널라이즈 때 지불(Unleashed 변경). 절충 유지 시 별도 조치 불필요.
- [B] 룰 355.12 ~ 355.13 (may perform on some number / up to) 'You may [행동] a unit' 형태 주문의 대상 지정 시점 (likely · 절충) — 절충 ④(이동 목적지 해결 시 선택)와 같은 계열. 'optional' op의 inner가 preTargetable이면 preTargetSpecs가 inner의 spec을 optional로 돌려 플레이 시 대상을 잡고, 수행 여부는 해결 시 묻도록 하면 규칙과 맞는다.
- [A] 룰 144.1.a~c (L886-890) 표준 이동 허용 시점 (메인 페이즈·열린 상태) (uncertain) — 선택 모달이 열린 동안 보드 입력이 실제로 통과하는지는 브라우저 검증이 필요해 uncertain. 안전하게는 executeMove/btn-move와 moveUnits에 G.phase==='action' 조건을 추가하면 된다.
- [A] 룰 190.6.d (L1429-1431) 무주공산 전장의 'When you defend here' 격발 (uncertain) — 규칙 본문은 구판(rules1.txt L1053~1054)과 동일하며 '통제자 없는 전장의 you는 아무도 아님'이라 문면상 무주공산 전장에서는 발동하지 않아야 하지만, 시뮬레이터는 RiftJudge #4985 판정을 따른다. 공식 판정과 규칙 문면이 충돌하는 항목이므로 재확인만 권고(
- [A] 룰 194.4 Winning (L1497-1506) 승리 조건 (클린업 판정 · 상대보다 많아야 함 · 동점 시 계속) (uncertain) — 구판 322.2 1단계는 '승점만큼이면 승리'였고 신판(Unleashed)은 '승점 이상 AND 모든 상대보다 많음'을 클린업에서 판정한다. 2인 대전에서는 득점이 한 번에 한 명씩 일어나고 즉시 판정하므로 결과가 갈리는 경우를 찾기 어렵다(동시 8점 도달은 사실상 불가). 다만 점수 
- [C] 룰 323 (Vendetta 클린업의 경합 해제/재적용 — 영역 B와 겹침) 경합 적용자의 유닛이 사라졌을 때 경합 해제 (uncertain) — 1v1에서 경합 전장은 다음 클린업에 곧바로 결전이 열려 종료 시 해제되므로, 남는 값은 '다른 전장의 결전 중 효과로 들어왔다가 되돌아간' 경우뿐이고 그때도 같은 플레이어가 다시 적용하게 되어 실효 차이는 확인되지 않았다. 엄밀히는 releaseEmptyBattlefields에서 `b
- [B] 룰 355 (2. Make choices) → 356 (3. Determine cost) → 357.1.a (4. Pay: Add Reactions) [반응] 자원 능력 사용 시점과 대상 지정 순서 (uncertain) — 정보 노출 순서만 다르다. 정리한다면 preTargetSpell을 자원 충당 루프 앞으로 옮기되, 굴절 후보 계산(canPayDeflect)이 '아직 충당 전' 자원을 보게 되므로 fundList 가능 자원을 canPayDeflect 계산에 포함해야 한다.
- [B] 룰 383.4.a.2 ~ 383.4.a.4 (once each turn + you may) '턴마다 한 번' 격발을 거부했을 때 횟수 소모 여부 (uncertain) — 구조적 차이만 기록. 향후 'once each turn, you may' 카드가 생기면 소모 표시를 실제 수행 뒤(거부·비용 미지불이면 미소모)로 옮겨야 한다.
- [E] 룰 416.4 Recycle (선택 시점) (uncertain) — 416.4(L4024-4030)는 'Recycle X from zone'이 대상 지정이 아니라고 하므로 원칙상 해결 시점에 고른다(그러면 비용으로 처치된 용광로 자신도 폐기장에 있어 후보가 됨). RiftJudge #11013은 반대로 판정했다고 주석에 있어 판정 근거가 '대상 지정'이
- [E] 룰 434.7.a Attach / Detach (탈진 상태 보존) (uncertain) — 부착/분리는 상태를 바꾸지 않는다(L4615-4620, 435). Origins 장비 도구가 탈진 능력을 가진 채 장착되는 경우가 있는지(장착 중 [E] 능력 사용 가능 여부 포함)는 700s(부착) 감사자 영역이라 여기서는 상태 보존 여부만 지적. u.gear에 {n,ex}를 저장하고
- [C] 룰 465.2.c.3 (대체 효과의 배분 단계 적용) 방지(Prevent)·피해 증폭 대체 효과를 치명 배분량에 반영 (uncertain) — 현재 카드 풀에서는 실효 차이 없음. 향후 '다음 피해 N 방지'·'피해 2배' 류가 유닛에 붙으면 배분 단계에서 lethal 계산에 반영해야 한다.
- [C] 룰 471.1 (Hold) 유지 득점의 '이번 턴 미득점' 조건 (uncertain) — 개시 격발이 정복까지 이어지는 Origins 카드가 실제로 있는지 확인되지 않아 uncertain. 수정은 한 줄: `if(bf.controller===p && !bf.scored[p] && G.winner===null)`.
- [C] 룰 476.2 / 323.2.b (승리 조건) 승리 판정 — 승점 도달 AND 상대보다 많음, 클린업 시점 (uncertain) — 1v1에서 양측이 같은 순간 8점이 되는 경우는 사실상 없어(점수가 V로 캡되고 먼저 도달한 쪽이 즉시 승리) 실효 차이는 거의 없다. 엄밀히 맞추려면 checkWin에 `P.points>opp.points` 조건을 넣고, 번아웃 경로가 아니면 cleanup 시점에 판정하도록 옮긴다.
- [D] 룰 811.1.d.2 / 811.1.d.2.a 숨김 도구는 그 전장에 놓인다 (uncertain) — 도구에 위치 개념이 없어 전장 배치가 표현되지 않는다. 존야의 효과는 위치와 무관하고, 엔진 주석대로 전장의 도구는 다음 클린업에 기지로 회수되므로(148) 실전 영향은 거의 없다. '전장의 도구' 참조 카드가 추가될 때만 문제.
- [D] 룰 811.1.d.3 (Each target is treated separately and individually) 숨김 대상 제한의 대상별 개별 적용 (uncertain) — Vendetta에서 '대상별 개별 적용'이 명문화됐다(Smoke and Mirrors 예시 — Origins 카드 아님). Origins의 [숨겨짐] 카드 9장 중 한 대상은 제한되고 다른 대상만 불가능한 혼합형이 확인되지 않아 현재 풀에서는 실질 차이 없음. 확장 시 hiddenFre
- [D] 룰 826 (Backline) / 815 (Tank) 케이틀린(68) '전투 피해를 마지막에 배분' = Backline 키워드 — 절충 ③의 전제가 바뀜 (uncertain · 절충) — CLAUDE.md 절충 ③은 '공식은 (마지막 배분 유닛도) 선택 허용'이라고 적혀 있으나, Unleashed에서 신설된 Backline(패치노트가 Caitlyn, Patrolling·Soraka, Wanderer의 능력을 이 키워드로 지목)은 '다른 비-Backline 유닛에 전부 치