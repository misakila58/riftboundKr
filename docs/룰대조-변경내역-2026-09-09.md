# 룰 대조 수정 내역 (2026-09-09) — RiftJudge 판정 196건 처리
수정 175 · 부분 수정 5 · 이미 해결됨 14 · 보류(룰북 미뒷받침) 2

## 1차 — 주문 격발 시점 · 플레이 카드 수 · 볼리베어 판정 · 오라 합산 (15건)
### #12 녹서스 지망생
- **수정** — 기존: 주문은 비용 지불 직후 playedCards++ 되어, 카운터당해도 [군단] 할인·'두 번째 카드' 조건이 성립했다
  → 변경: 주문의 playedCards는 해결 시점(fireSpellPlayEvents)에서만 증가 — 카운터된 주문은 세지 않음(유닛·도구는 종전대로 즉시)
  _근거: 351.3 "A card is Played when it has finished this process in its entirety" · 408.2 "countered--the card wasn't played" · Counter "not considered to have been played"_
### #15 패론 대위
- **수정** — 기존: kwAura가 base[k]=base[k]||true 로 덮어써 패론 2기여도 [맹공 1], 기존 [맹공 2] 유닛은 그대로 2
  → 변경: 오라의 수치 키워드(assault/shield/deflect)는 addKw로 합산 — 패론 2기 = [맹공 2], [맹공 2]+패론 = 3
  _근거: Assault 조항 "granted Assault by an additional source, the Assault Value of all granted Assault keywords is summed"_
### #16 위험한 2인조
- **수정** — 기존: onPlay 격발(자기 +2)을 먼저 해결한 뒤 isMighty를 검사해 볼리베어가 격발했다
  → 변경: isMighty 검사를 placeUnit 직후·onPlay 격발 전으로 이동 — 자기 버프로 5⚔가 되는 유닛은 격발하지 않음
  _근거: 375 Play Effects "put on the Chain as Pending Items after the Permanent ... is finalized and enters the board" · 376.2.a 격발 조건은 사건 처리 직후 평가_
### #21 태양 원반
- **보류** — 기존: 태양 원반을 그 턴 첫 카드로 내고 바로 탈진해 [군단] 효과를 쓸 수 있다
  → 변경: 변경 없음
  _근거: Legion 조항 "On other abilities and instructions, it is functionally short for 'If you have played a Main Deck card this turn'" (rules1.txt 2025-12-01)_
  _보류 사유: 보유 룰북(2025-12-01)은 '다른 카드'(another) 조건을 주문·등장 효과에만 두고, 활성화 능력 등 그 외에는 '이 턴에 메인 덱 카드를 플레이했는가'로 규정한다 — 원반 자신의 플레이가 이를 만족하므로 판정(812.1.c 기반 최신 룰)과 룰북이 상충. 룰북 갱신 후 재검토_
### #52 굳건한 포로
- **수정** — 기존: 타릭 오라(kwAura shield)가 이미 [보호막 1]인 포로에 합산되지 않아 방어 시 +1뿐
  → 변경: 오라 [보호막]이 합산되어 포로 방어 시 [보호막 2] (2⚔→4⚔)
  _근거: Shield 조항 "granted Shield by an additional source, the Shield Value of all granted Shield keywords is summed" (예시: Stalwart Poro + Block = Shield 4)_
### #54 햇살의 수호자
- **수정** — 기존: 타릭 오라 몫(+1)이 빠져 [보호막 1]+막기 [보호막 3] = 4에 그침
  → 변경: 자체 1 + 오라 1 + 막기 3 = [보호막 5] (패론 대위 건과 같은 수정)
  _근거: Shield 조항 "the Shield Value of all granted Shield keywords is summed"_
### #64 바람 장막
- **수정** — 기존: 중립/결전에서 대상 주문이 없어도 비용을 내고 '효과 없이 폐기'되며 '주문 플레이' 트리거·playedCards가 발생
  → 변경: playRestriction이 counterTargets(체인의 적법 상대 주문)가 없으면 거부 — 비용 지불·격발 없음(봇은 실패를 tried로 처리)
  _근거: 352 Targeting "In order to put a spell or ability on the chain, valid choices must be made for all targets" · "'Spell' and 'ability' refer to objects on the Chain" · 737 숨김 주문 동일_
### #80 신비한 반전
- **수정** — 기존: 탈취돼도 fireSpellPlayEvents(p)가 원 시전자 기준 — 원 시전자의 playedCards·다리우스·[군단]이 성립
  → 변경: fireSpellPlayEvents(execAs) — 해결 시 통제자(탈취자)가 플레이한 것으로 셈·격발, 원 시전자는 셈하지 않음
  _근거: 155.2 "A spell is controlled by the player who played it" · 351.3 플레이는 과정 완료(해결) 시점 · 407.3.a "trigger when the act of playing the card has been completed by the resolution"_
### #83 과거에게 묻다
- **수정** — 기존: 숨김 주문의 onPlayFromHidden이 체인 적재 시(결전) 또는 카운터 여부와 무관하게(중립) 발화
  → 변경: 주문의 onPlayFromHidden은 fireSpellPlayEvents(해결 시점)에서만 — 체인 항목에 fromHidden 보관, 카운터되면 발화 없음(유닛·도구는 종전대로)
  _근거: 408.2 "countered--the card wasn't played and no abilities that trigger on playing cards trigger" · 407.3.a_
### #103 레이븐블룸 학생
- **수정** — 기존: resolveSpellEffects가 playOps 실행 전에 onYouPlaySpell/onYouPlayCard를 내어 학생 +1이 효과보다 먼저 붙음(돌풍 불발·자기 피해 주문에서 생존)
  → 변경: 주문 효과 실행·폐기 → cleanupDeaths(치명 판정) → fireSpellPlayEvents 순서로 변경
  _근거: 407.3.a "trigger when the act of playing the card has been completed by the resolution of the card" · 356.3.e.11 예시(주문이 해결되며 격발) · 319.7/322 2a 체인 이탈 직후 클린업_
- **수정** — 기존: 탈취 시 원 시전자의 학생이 +1을 받음
  → 변경: 탈취자의 학생이 +1 (fireSpellPlayEvents를 execAs로 호출)
  _근거: 155.2 · 351.3 · 407.3.a (신비한 반전 건과 동일)_
### #167 잉걸불 수도승
- **수정** — 기존: 숨김 주문이 카운터돼도 +2, 결전 중에는 주문 해결 전에 +2
  → 변경: 숨김 주문이 해결된 뒤에만 +2, 카운터되면 없음
  _근거: 408.2 · 407.3.a (과거에게 묻다 건과 동일 원인)_
### #203 빙의
- **수정** — 기존: onYouPlayCard가 possess 실행 전에 나서 뺏어온 다리우스가 '두 번째 카드'를 못 봄
  → 변경: 효과 실행 뒤 격발 — 빙의를 두 번째 카드로 내면 뺏어온 다리우스가 준비·+2
  _근거: 407.3.a (레이븐블룸 학생 건과 동일 원인)_
### #294 삼두정 전쟁 야영지
- **수정** — 기존: 등장 격발로 +위력을 얻어 5⚔가 되는 유닛(세트·숨김 티모)도 볼리베어 격발
  → 변경: 등장 직후(상시 효과만 반영된 상태)에 판정 — 야영지 상시 +1로 5⚔면 격발, 등장 격발 버프는 미격발
  _근거: 375 Play Effects · 376.2.a (위험한 2인조 건과 동일)_
### #315 선봉대 소집
- **수정** — 기존: vanguardTokens·token op이 placeUnit만 하여 오라 [통찰]·'유닛 플레이' 리스너(시트리아)가 토큰에 반응하지 않음
  → 변경: tokenPlayed 헬퍼: 토큰 배치 후 effKw(u).vision이면 visionCheck, onYouPlayUnit 발화(카드가 아니므로 playedCards·onYouPlayCard 제외). 순서상 [통찰]→주문 플레이 격발(럭스 드로우)도 판정과 일치
  _근거: 351.3 "Tokens are not cards, but can still be Played" · 743.2.b Vision "The trigger is the permanent entering the Board"_

## 2차 — 플레이 시점 대상 지정(전용 op) · 적법 대상 · 굴절 (24건)
### #4 가르기
- **수정** — 기존: grantKw(who:'a unit')는 PRE_TARGET_OPS에 없어 해결 시점에 아무 유닛이나 골랐다 — 돌풍으로 원 대상이 손패로 가면 다른 유닛에 [맹공 3]
  → 변경: preTargetSpecs가 grantKw(who≠me/it)에 grantKwSpec을 돌려 플레이 시점 지정 — 대상 소실 시 그 지시만 불발(부여 주문 전부 공통)
  _근거: 352.8.a "If a card requires you to specifically choose one or more Game Objects, that choice is made now" · 356.3.e.4 존 이동 대상은 부적법_
### #8 신난다!
- **수정** — 기존: dmgLastDiscardCost가 spec 없는 전용 op라 해결 시(버릴 카드를 본 뒤) 대상을 골랐다
  → 변경: PRE_TARGET_EXTRA.dmgLastDiscardCost — 대상은 적재 시점, 버림은 해결 시점, 대상 소실이어도 버림은 실행·피해만 불발
  _근거: 352.8.a 플레이 시점 대상 지정 · 356.3.e.2 "The spell resolves even if some or all of its targets are illegal"_
### #13 뾰로통한 포로
- **수정** — 기존: 떨어지는 별·이케시아 소나기가 플레이 시점에 대상·굴절을 확정해 응수로 돌풍을 맞으면 굴절 낭비, 대상 변경 불가
  → 변경: 두 카드 fx.reflexive — 플레이 시 대상 없음(굴절 없음), 해결 시점에 지시마다 대상·굴절 결정, 지불 거부 시 그 지시만 소멸(#233 '트리거를 죽일 수 있다'와 일치)
  _근거: 383 반사 격발 · 352.8.b · 735 굴절은 '고를 때' 추가 비용_
### #24 공허의 추적자
- **수정** — 기존: 손패 플레이에 대상 존재 검사가 없어 전장 유닛 0이어도 플레이·드로우 1만 실행
  → 변경: spellHasTargets(구 hiddenSpellHasTarget 일반화)를 playRestriction이 호출 — 필수 대상 spec 하나라도 후보 0이면 플레이 불가, 봇 polCanPlay도 동일 판정
  _근거: 352.8 "In order to put a spell or ability on the chain, valid choices must be made for all targets"_
- **수정** — 기존: 플레이 시점 굴절 거부 시 pre에 null이 남아 대상 없이 플레이·해결(드로우만)
  → 변경: pickPreTarget: 굴절을 본 비용과 합산해 낼 수 없는 유닛은 후보 제외, 거부하면 그 유닛을 빼고 재선택, 남은 후보가 없으면 PRE_CANCEL로 플레이 취소(비용 지불 전이라 되돌릴 것 없음 — preTargetSpell을 352 선택→353 비용 순서로 이동)
  _근거: 735.3 "Mandatory Additional Cost on Spells and Abilities that choose the permanent" · 353.2.b.1 · 352.8_
### #29 떨어지는 별
- **수정** — 기존: 굴절 미지불/불가 시 다른 적법 대상이 있어도 그 지시 전체 생략
  → 변경: 떨어지는 별은 반사 격발(fx.reflexive)이라 해결 시점 대상 — 거부 시 그 격발만 소멸(#233 판정). 플레이 시점 대상 주문은 pickPreTarget이 거부 시 다른 대상 재선택을 요구
  _근거: 735.3 인스턴스마다 굴절 · 383 반사 격발_
### #33 갈취
- **수정** — 기존: extortion이 해결 시 pickBySpec — 적 유닛 없어도 플레이, 응수로 대상이 사라지면 재지정
  → 변경: PRE_TARGET_EXTRA.extortion — 적 유닛 없으면 플레이 불가, 적재 시점 대상 고정
  _근거: 352.8 대상 필수 · 352.8.a 플레이 시점 지정_
### #46 앙 가르드
- **수정** — 기존: engarde가 해결 시 대상 선택 — 저항으로 무효화되면 꿈꾸는 나무 드로우 없음, 원 대상 사망 시 다른 아군에 +1/+2
  → 변경: PRE_TARGET_EXTRA.engarde — 적재 시점 지정(noteSpellPick·굴절도 그때), 대상 소실 시 불발
  _근거: 352.8.a 플레이 시점 지정 · 356.3.e_
### #53 단결된 의지
- **수정** — 기존: 아군 유닛 0이어도 플레이 가능(buffPlus 플래그만 실행)
  → 변경: spellHasTargets로 아군 없으면 플레이 불가 (buff count:1 spec)
  _근거: 352.8 대상 필수_
### #57 막기
- **수정** — 기존: grantKw 해결 시 선택 — 상대는 누가 [보호막 3]/[탱커]를 받을지 모른 채 응수
  → 변경: 가르기와 같은 수정(grantKwSpec) — 적재 시점 지정, 유닛 없으면 플레이 불가
  _근거: 352.8.a 플레이 시점 지정_
### #95 혼미
- **수정** — 기존: 유닛 없어도 플레이해 드로우 1만 얻음
  → 변경: spellHasTargets — 유닛 없으면 플레이 불가
  _근거: 352.8 대상 필수 ("do as much as you can"은 해결 시에만 — 356.3.e)_
### #104 후퇴
- **수정** — 기존: retreatOp이 해결 시 chooseReturnToHand로 새로 골라 후퇴에 후퇴로 룬 2개 전개 가능
  → 변경: PRE_TARGET_EXTRA.retreatOp + chooseReturnToHand가 takePreTarget 소비 — 대상 소실 시 룬 전개 없음·재지정 불가
  _근거: 352.8.a 플레이 시점 지정 · 356.3.e.4 "changed Zones to or from a Non-Board Zone" → 부적법_
### #108 수렴 변이
- **수정** — 기존: 복사 기준 '다른 아군'을 해결 시 UI.pickUnitFrom으로 선택 — 아군 1기여도 플레이 가능
  → 변경: PRE_TARGET_EXTRA.mightSetToOther(prev 기반 _exclude) — 두 유닛 모두 적재 시점, 아군 2기 미만이면 플레이 불가
  _근거: 352.8 모든 대상 지정 후 체인 적재_
### #128 도전
- **수정** — 기존: challenge가 해결 시 아군·적을 골라 원 대상이 죽어도 다른 유닛으로 대체
  → 변경: PRE_TARGET_EXTRA.challenge(2대상 배열) — 한쪽 소실 시 양쪽 모두 피해 없음
  _근거: 352.8.a · 356.3.e_
### #169 돌풍
- **수정** — 기존: bounceSpec이 해결 시 '그때 3위력 이하'에서 새로 골라 사실상 재지정
  → 변경: PRE_TARGET_EXTRA.bounceSpec(bounceTargetSpec, returnHand 선택지 유지) — 응수로 4위력이 되면 그 지시만 불발
  _근거: 356.3.e.3 "A target is illegal as the spell resolves if it no longer meets the targeting requirements"_
### #172 질책
- **수정** — 기존: 해결 시 대상 선택 — 후퇴로 빼면 재지정, 굴절도 해결 시점
  → 변경: 돌풍과 같은 bounceSpec 수정 — 적재 시점 지정·굴절 지불, 소실 시 불발
  _근거: 352.8.a · 356.3.e.4_
### #203 빙의
- **수정** — 기존: possess가 해결 시 선택 — 기지로 도망간 유닛 대신 다른 유닛 선택 가능
  → 변경: PRE_TARGET_EXTRA.possess — 적재 시점 지정, 기지로 가면 불발
  _근거: 352.8.a · 356.3.e.3 ("unit at a battlefield" 조건 불충족 → 부적법)_
### #220 안면 분쇄
- **수정** — 기존: UI.pickUnitFrom 직접 호출 — 굴절·꿈꾸는 나무·_hiddenBf 전장 제한 모두 누락
  → 변경: facebreakerSpec(적: 아군과 함께 있는 전장 / 아군: 그 전장) + pickBySpec 2회 — 적재 시점 지정, 굴절·737 적용
  _근거: 352.8.a (지정=대상) · 735 굴절 · 737 숨김 대상 제한_
- **수정** — 기존: 같은 전장 쌍이 없어도 플레이(비용 지불·폐기)되고 아무도 기절하지 않음
  → 변경: spellHasTargets가 facebreakerSpec 후보를 보므로 쌍이 없으면 손패·숨김 모두 플레이 불가
  _근거: 352.8 대상 필수_
### #248 이케시아 소나기
- **부분 수정** — 기존: damage×6이 PRE_TARGET_OPS라 플레이 시점에 6대상·굴절 확정, 응수로 공개된 유닛은 대상 불가
  → 변경: SCRIPTS[248].reflexive=true — preTargetSpell·spellHasTargets 제외, 대상은 해결 시점(절충 ①: 격발 즉시 해결)에 고르고 굴절도 그때. 남은 차이: 반사 격발이 「꿈꾸는 나무」를 격발하지 않아야 하나 noteSpellPick은 _curKind='spell'이라 드로우함(사소·미수정)
  _근거: 383 "'Do this' can be followed by 'N times.' The Reflexive Trigger will thus be added to the chain N times" · 352.8.b 격발 능력의 선택은 플레이 시점이 아님_
### #254 녹서스의 단두대
- **수정** — 기존: guillotine이 해결 시 선택 — 상대가 대상을 모른 채 응수, 굴절·꿈꾸는 나무도 해결 시점
  → 변경: PRE_TARGET_EXTRA.guillotine — 적재 시점 지정
  _근거: 352.8.a 'choose' = 대상_
### #258 용의 분노
- **부분 수정** — 기존: dragonRage가 유닛·목적지·상호 피해 상대를 모두 해결 시 chooseEffectMove로 선택
  → 변경: PRE_TARGET_EXTRA.dragonRage + chooseEffectMove가 takePreTarget 소비 — 이동 유닛은 적재 시점 고정, 소실/이동 불가면 이동·피해 모두 없음. 목적지는 해결 시점(절충 ④), '도착지의 다른 적'은 반사 격발이라 해결 시점(판정과 일치)
  _근거: 352.8.a 이동시킬 유닛은 플레이 시점 대상 · 352.7 이동 목적지(절충 ④ 유지)_
### #268 쌍권총 난사
- **수정** — 기존: gunsBlazing이 해결 시 힘 지불 후 전장 선택 — 상대가 어느 전장인지 모른 채 응수
  → 변경: spec {battlefield:true}로 적재 시점에 전장을 고르고 {bf} 저장, 해결 시 힘 액수·지불만. 봇의 미스 포츈 난사 플랜은 option(전장)→number(액수) 순서로 재배선
  _근거: 352.10.d.1 "'Kill all units at a battlefield' targets a battlefield" → 전장은 대상 · 352.8.a 플레이 시점_
### #308 신사의 결투
- **수정** — 기존: gentlemenDuel이 아군·적 둘 다 해결 시 UI.pickUnitFrom으로 선택(굴절 없음)
  → 변경: PRE_TARGET_EXTRA.gentlemenDuel + pickBySpec 2회 — 적재 시점 지정·굴절, 아군 소실 시 적 피해 없음
  _근거: 352.8.a — 문장 중간의 '그 뒤 고른다'도 플레이 시점 대상_

## 3차 — 결전 정리 · 공격/방어 지정 · 유닛 피해 종류 · 불사조 (46건)
### #8 신난다!
- **수정** — 기존: 피해 주문의 치명 사망은 G._casting=null 뒤의 cleanup에서 일어나 _spellKilled가 서지 않음 → 불사조 미격발
  → 변경: dealDamage가 마지막 피해의 시전자를 u._spellDmgBy에 기록, cleanupDeaths의 killUnit(byDamage)이 그 시전자를 G._spellKilledBy에 적고 cleanupDeaths 끝의 spellKillReactions가 불사조를 연다
  _근거: 416 "When one or more Units is killed due to a Cleanup, the spell or ability that resolved immediately prior to that Cleanup that applied damage to the Unit ... is responsible for that Kill action"_
### #9 마법공학 광선
- **수정** — 기존: 광선으로 죽인 유닛은 불사조를 격발시키지 않음(처치 op만 격발)
  → 변경: _spellDmgBy → G._spellKilledBy 경로로 격발 (test-batch3 '광선 처치 → 불사조 프롬프트')
  _근거: 416 클린업 사망의 처치 귀속 = 직전에 피해를 준 주문_
- **수정** — 기존: 무혈 결전 종료 치유
  → 변경: #311과 동일
  _근거: 142.4_
### #13 뾰로통한 포로
- **수정** — 기존: 위와 동일 원인
  → 변경: 위와 동일 수정
  _근거: 416 (위와 동일)_
### #29 떨어지는 별
- **수정** — 기존: 존야가 배치 첫 유닛 자동 구원
  → 변경: #77 수정으로 함께 해소
  _근거: 376.3 동시 충족 시 통제자 선택 (#77)_
- **수정** — 기존: 내 주문으로 내 유닛(불사조) 처치 시 미격발
  → 변경: 진영 조건 제거
  _근거: 376.2.c 자기 유닛 처치 포함_
### #37 불멸의 불사조
- **수정** — 기존: killUnit이 u.ctrl!==G._casting일 때만 _spellKilled — 자기 유닛(불사조 자신 포함) 처치는 미격발
  → 변경: 처치 지시는 진영 무관하게 시전자(G._casting)의 주문 처치로 귀속; 반응 검사는 폐기장 상태 기준이라 자기 불사조도 연다
  _근거: 376.2.c 예시 "This ability triggers if Immortal Phoenix is in your trash immediately after you kill a unit with a spell, even if the unit you killed with a spell was that Immortal Phoenix"_
- **수정** — 기존: 피해 주문(광선·보이드 시커·떨어지는 별·폭풍을 부르는 자 등)으로는 절대 격발 안 됨
  → 변경: _spellDmgBy 귀속으로 피해 주문·부분 피해 후 마무리 주문 모두 격발 (위력 감소만으로 죽은 경우는 _spellDmgBy가 없어 제외)
  _근거: 416 클린업 사망 귀속 · 405.1 "The damage that Void Seeker instructs you to deal is dealt by Void Seeker"_
- **수정** — 기존: makeUnit+placeUnit으로 항상 기지에만 배치, 플레이 경로(위치 선택·등장 격발) 미경유
  → 변경: playFromTrash와 같은 정식 경로(손패 맨 앞→playCardFromHand fromTrash, 비용은 미리 지불) — 기지/통제 전장(방어 중 포함) 선택, 공격 중인 전장은 통제가 아니라 불가
  _근거: 441 "Battlefields with Staged Combats or Combats in Progress are Invalid to be chosen as a location to play one or more Units by a player not involved" (참여자는 통제 전장 가능) · 유닛 플레이 기본 위치 = 기지 또는 통제 전장_
- **수정** — 기존: new Set(P.trash)로 카드 번호당 1회만 프롬프트
  → 변경: 폐기장의 장수만큼 각각 비용을 내고 플레이 (거절하면 중단)
  _근거: 376.2.c 격발은 게임 오브젝트마다 개별 평가 ("they all trigger individually" #6871)_
### #55 물을 다루는 자
- **수정** — 기존: 전투 사망의 종소리를 killUnit 안에서 즉시 실행한 뒤 다음 줄에서 전 유닛 치유 → 종소리 피해가 지워짐
  → 변경: resolveShowdown이 _deferDeathFx로 종소리·사망 이벤트를 보류, 치유·귀환·통제 확립 뒤에 해결하고 cleanup이 치명 판정 (전투 종료 후라 방어자 보정 없음)
  _근거: 322 "While a Cleanup is occurring, Chain Items cannot be Resolved. New Pending Items can be added" · 전투 해결 1단계 Combat Cleanup 2c "Heal all Units"_
### #60 예지의 가면
- **수정** — 기존: startShowdown이 hasCombat과 무관하게 혼자인 유닛에 +1 (무혈→전투 전환 시 +2)
  → 변경: designateUnits(전투 결전에서만 호출)에서 지정 순간에만 적용
  _근거: 378 "Attack Triggers ... trigger when a Unit gains the Attacker designation" · 전투 1단계 지정은 Combat에서만 · 437 "A Combat occurs when ... a Battlefield has Units controlled by two opposing players"_
- **수정** — 기존: gear.some(...)으로 장수 무관 +1
  → 변경: gearAloneCombat 도구 수만큼 +N
  _근거: 376 격발 능력은 출처마다 개별 격발 (#6946 "Both Mask of Foresight copies will trigger")_
- **수정** — 기존: 야스오 onAttack이 이동 직후(fireAttackTriggers) 해결되고 가면 +1은 그 뒤 startShowdown에서 붙음
  → 변경: 가면은 지정 시점(designateUnits)에 먼저 적용되고 [공격 시]는 그 뒤 해결 → 야스오 피해에 가면 반영 (공격자가 가면을 먼저 해결하는 순서로 고정)
  _근거: 전투 1단계 "The Attacking player, who has Focus, places Triggered Abilities on the Chain first" (같은 초기 체인, 통제자가 순서 결정)_
- **수정** — 기존: 가면은 startShowdown에서만 검사
  → 변경: fireAttackTriggers→designateUnits가 결전 중 합류 유닛에도 '처음 지정 시 혼자인가'를 검사
  _근거: 전투 1단계 "becomes present at this Battlefield after this moment ... will gain the ... designation during the Cleanup" · 378_
### #76 야스오 - 회한에 잠긴
- **수정** — 기존: fireAttackTriggers가 '적이 있으면 공격'으로 판정 → 바람 타기로 합류한 야스오(방어자)의 [공격 시] 6피해
  → 변경: fireAttackTriggers는 진행 중인 '전투' 결전(hasCombat)에 들어온 유닛만 자기 진영(sd.attacker/defender) 지정으로 처리; 무혈 결전 합류는 전환 시 startShowdown이 지정
  _근거: 351.1 "The player who applied Contested to the Battlefield will be the Attacker" · 전투 1단계 "Units at the Contested Battlefield controlled by the Defender gain the Defender designation"_
- **수정** — 기존: 적이 있는 전장에 들어올 때마다 onAttack
  → 변경: G.showdown.designated[uid]로 한 전투 1회 (test-batch3 '재진입: [공격 시] 재격발 없음')
  _근거: 378 "These triggers can only become fulfilled once per combat, despite a Unit being able to gain and lose the Attacker designation multiple times"_
### #77 존야의 모래시계
- **수정** — 기존: killUnitsTogether가 리스트 순서대로 killUnit → 배치의 첫 아군이 자동 구원
  → 변경: killUnitsTogether가 배치에 존야 수보다 많은 아군이 있으면 통제자에게 pickUnitFrom으로 회수할 유닛을 먼저 묻고 그 유닛부터 처리 (봇: '사망 방지' 문구 → POL_BENEFIT 최강 유닛)
  _근거: 376.3 "If an ability triggers 'the [Nth] time' something happens and that trigger condition is met multiple times simultaneously, the ability's controller picks one of those instances" ("The next time" 대체 효과에 같은 원칙 — #10768) · 370.2 대체 효과 순서는 통제자_
### #96 경계하는 보초
- **수정** — 기존: eachPlayerKills가 턴 플레이어 유닛부터 따로 killUnit
  → 변경: 두 유닛을 고른 뒤 killUnitsTogether([비턴, 턴]) 한 배치 — 비턴 플레이어 종소리부터 해결
  _근거: 376.3.b 함께 죽는 유닛은 서로의 죽음을 못 봄 · 전투 1단계/초기 체인 "Turn Player places their trigger on the chain first" → LIFO_
### #105 특이점
- **수정** — 기존: 동시 사망 배치의 첫 유닛 자동 구원
  → 변경: #77 수정으로 함께 해소
  _근거: 376.3 동시 충족 시 통제자 선택 (#77과 동일)_
### #121 티모 - 전략가
- **수정** — 기존: onDefend는 startShowdown 시점의 방어측 유닛에만 → 결전 중 숨김 공개/효과 이동으로 합류한 티모 미격발
  → 변경: fireAttackTriggers(플레이·효과 이동·토큰 공통)가 전투 결전 합류 유닛에 진영별 지정 격발(onDefend/onAttack)을 낸다. 에라타 원문대로 onDefend만 등록(숨김 플레이 경로 별도 없음)
  _근거: 전투 1단계 "If a Unit controlled by the Defender becomes present at this Battlefield after this moment it will gain the Defender designation during the Cleanup phase" · 378 Defend Triggers_
### #127 포탄 세례
- **수정** — 기존: where:'combat'이 G.showdown만 검사 → 무혈 결전의 유닛에도 2피해
  → 변경: unitsBySpec where:'combat'에 G.showdown.hasCombat 조건 추가
  _근거: 437 "A Combat occurs when ... a Battlefield has Units controlled by two opposing players" — 무혈 결전은 전투가 아님_
### #128 도전
- **수정** — 기존: challenge op가 kind 'spell' → 불굴의 정신이 막고 레이븐본 서적 보너스가 붙음
  → 변경: 새 kind 'unit' — dealDamage의 preventSpellDmg 검사와 nextSpellBonus에서 제외, 불사조 귀속(_spellDmgBy)도 없음
  _근거: 406 "When a spell or ability specifies a Unit as the source of the Damage ... it is not in addition to the spell or ability" — Challenge 예시 "dealt by the chosen units, not by Challenge"_
- **수정** — 기존: might(a)/might(b)를 combatRole 없이 호출 → 전투 중 [맹공]·[보호막]·마스터 이 보정 누락
  → 변경: challenge·gentlemenDuel·fightMutual·itDealsTo·dmgEqMyMight·dragon 모두 targetMight(결전 전장이면 role 포함, forKill)
  _근거: 443.1.d "using their current Might" · Assault "remains in effect as long as the Unit maintains the Attacker designation"_
- **수정** — 기존: resolveShowdown이 hasCombat 무관하게 everyUnit 치유
  → 변경: 치유·전투 부여 해제·공격자 귀환은 sd.hasCombat일 때만
  _근거: 142.4 "Damage is Healed from Units at two specific times: At the end of each player's turn. During a Combat Cleanup."_
### #145 불굴의 정신
- **수정** — 기존: kind!=='combat' 전부 방지 → 도전·육식 덩굴·최후의 숨결·신사의 결투·용의 분노까지 방지
  → 변경: kind 'unit'은 방지 대상에서 제외; 보이드 시커·야스오("deal damage equal to my Might" — 주어 없음)·폭풍을 부르는 자·케이틀린·발리스타는 그대로 방지
  _근거: 406 유닛 출처 피해는 주문/능력 피해가 아님 · 카드 원문 "Prevent all spell and ability damage"_
### #168 투쟁 혹은 도피
- **수정** — 기존: 무혈 결전에도 가면 +1, 야스오 onAttack이 약탈자의 골목 onDefendHere보다 먼저
  → 변경: 가면은 전투 지정 시만; startShowdown이 onDefendHere → 방어측 유닛 격발 → 공격측 onAttack 순으로 해결
  _근거: 전투 1단계 지정은 Combat에서만(가면) · "Attacking player ... places Triggered Abilities on the Chain first, followed by ... the Defending Player" → 방어자 격발 선해결_
### #173 바람 타기
- **수정** — 기존: 합류 방어자의 [공격 시]+[방어 시] 둘 다 발동, 원래 공격자의 [공격 시]는 영영 없음
  → 변경: 무혈 결전 중엔 지정 없음; resolveShowdown→startShowdown(hasCombat)에서 공격측 onAttack·방어측 onDefend를 한 번에 지정(방어측 먼저 해결)
  _근거: 351.1 공격자 = 경합 적용자 · 378 지정을 처음 받을 때 격발 · 349.1 무혈 결전 후 양측 잔존 → Staged Combat_
### #176 교활한 갑판원
- **수정** — 기존: 포탄 세례가 빈 전장 결전의 갑판원에게 피해
  → 변경: #127과 동일 수정
  _근거: 437 (무혈 결전은 전투 아님)_
### #199 물결을 바꾸는 자
- **수정** — 기존: effectMove 후 fireAttackTriggers가 방어측 교환 유닛에 onAttack을 내고 onDefend는 안 냄
  → 변경: 합류 유닛은 진영별(onAttack/onDefend) 지정 격발 (test-batch3 티모 합류 [방어 시])
  _근거: 전투 1단계 방어자 측 합류 유닛의 Defender 지정 · 378 Defend Triggers_
### #209 약자 도태
- **수정** — 기존: 내 약자 도태로 내 불사조만 죽으면 미격발
  → 변경: 처치 지시 귀속에서 진영 조건 제거 (상대 주문으로 내가 고른 처치는 상대 폐기장만 검사 — #6480 그대로)
  _근거: 376.2.c 자기 유닛 처치도 '주문으로 처치'_
### #213 숨겨진 칼날
- **수정** — 기존: u.ctrl!==G._casting 조건으로 자기 불사조 처치 콤보 불가
  → 변경: 조건 제거 — test-batch3 '자기 유닛 처치: 불사조 재플레이'
  _근거: 376.2.c 예시 (불사조 자신을 처치해도 격발)_
### #221 황제의 칙령
- **수정** — 기존: 치명 피해+칙령 표식을 killUnit 한 번으로 처리, 대체 효과가 회수하며 _decree를 지움 → 생존
  → 변경: killUnit이 대체로 살아난 뒤 wasLethal&&hadDecree면 칙령 처치를 한 번 더 시도(비치명이면 칙령 처치 하나뿐이라 대체로 생존 #8420)
  _근거: 322 2b 클린업 처치와 416 처치 지시(칙령 격발)는 별개의 Kill 사건 · 370.2 대체 효과는 '그 사건'만 대체_
### #227 솔라리의 상징
- **수정** — 기존: '전원 사망'을 무승부로 보고 시체를 귀환(사망 무효화), 양측 잔존 시엔 아무것도 안 함
  → 변경: 양측 잔존(무승부)이고 공격측이 227 보유면 전 유닛 귀환 → 통제 변경·득점 없음; 사망은 정상 처리
  _근거: 전투 해결 Combat Cleanup 2d "Recall Attackers present at the Battlefield if Defenders are still present" — 카드 원문 "If a combat where you are the attacker ends in a tie, recall ALL units instead"_
### #229 복수
- **수정** — 기존: 내 복수로 내 유닛 처치 시 미격발
  → 변경: 처치 지시 귀속에서 진영 조건 제거
  _근거: 376.2.c (자기 유닛 처치 포함)_
### #250 폭풍을 부르는 자
- **수정** — 기존: 피해로 죽인 유닛은 미격발
  → 변경: _spellDmgBy 귀속으로 격발 (피해 kind는 'spell' 유지 — 주어 없는 주문 피해라 불굴의 정신에 막힘 #1999)
  _근거: 416 클린업 사망 귀속_
- **수정** — 기존: cleanupDeaths가 치명 유닛을 한 번만 추림 → 리 신 사망으로 오라를 잃은 유닛이 다음 클린업까지 생존
  → 변경: cleanupDeaths가 변화가 없을 때까지(최대 8회) 치명 판정을 반복
  _근거: 322 "If an event occurs during a Cleanup that qualifies for a Cleanup, another Cleanup will occur immediately after the first completes, repeating until a Cleanup occurs with no new change"_
### #254 녹서스의 단두대
- **수정** — 기존: 표식 처치가 cleanup에서 G._casting=null로 일어나 불사조 미격발
  → 변경: u._guillotineBy/_decreeBy(TF().dmgKillBy)에 시전자를 기록, killUnit이 표식 처치를 그 시전자에게 귀속 → cleanupDeaths 끝에서 반응
  _근거: 416 "A spell or ability that contains a Kill instruction is responsible for Killing" — 지연 처치도 단두대의 처치 (#7364)_
### #258 용의 분노
- **수정** — 기존: resolveEffectMove dragon이 'spell' kind → 불굴의 정신이 방지
  → 변경: kind 'unit' + targetMight
  _근거: 406 유닛 출처 피해 ("They deal damage equal to their Mights to each other")_
### #260 최후의 숨결
- **수정** — 기존: itDealsTo가 kind 'spell'
  → 변경: kind 'unit' + targetMight (dmgEqMyMight는 주어 없는 능력 피해라 'ability' 유지)
  _근거: 406 ("It deals damage equal to its Might" — 유닛이 출처)_
### #262 천공의 검
- **수정** — 기존: 방어자로 합류해도 [공격 시]와 상대 전설(hookEnemyAttackMyBf) 격발
  → 변경: 합류 유닛은 sd.attacker/defender에 따라 지정; hookEnemyAttackMyBf는 공격자 지정 + 방어자가 통제하는 전장일 때만(빈 적 전장 무혈 점거에는 미격발)
  _근거: 전투 1단계 공격자 지정 = 경합 적용자 측 유닛만 · 378 · 아리 원문 "When an enemy unit attacks a battlefield you control"_
### #285 약탈자의 거리
- **수정** — 기존: onDefendHere 이동 뒤에 가면의 '혼자' 판정
  → 변경: designateUnits가 격발 전에 aloneAt 스냅샷 → 그 뒤 onDefendHere → 유닛 격발
  _근거: 376.2.c "The Condition of a Trigger is evaluated after a potentially inciting event has been processed" — 지정 순간의 상태로 동시 격발_
### #294 삼두정 전쟁 야영지
- **수정** — 기존: 종소리 피해가 치유에 지워짐
  → 변경: #55와 동일 수정 (test-batch3 코그모 종소리)
  _근거: 322 "Legal Items cannot be executed" during Cleanup · Combat Cleanup 2c Heal 뒤 보류 항목 해결_
### #296 공허의 관문
- **수정** — 기존: 도전 피해가 방지되고 서적 보너스가 붙고 불사조가 열림
  → 변경: kind 'unit'은 방지·보너스·_spellDmgBy 모두 제외 (test-batch3 '도전 처치는 주문 처치가 아니다')
  _근거: 406 Challenge 예시 · 416 처치 귀속_
### #308 신사의 결투
- **수정** — 기존: gentlemenDuel이 'effect' kind → 방지됨
  → 변경: kind 'unit' + targetMight
  _근거: 406 ("They deal damage ... to each other")_
### #311 점멸
- **수정** — 기존: 무혈 결전 종료에도 전 유닛 치유
  → 변경: sd.hasCombat일 때만 치유 (전투가 열린 뒤 한쪽이 빠져나간 경우는 hasCombat 유지라 치유 — #10674 일치)
  _근거: 142.4 치유는 턴 종료와 Combat Cleanup에서만_

## 4차 — 덱·폐기장·효과 플레이 경로 통일 · 녹턴 · 시간 왜곡 (20건)
### #62 증원
- **부분 수정** — 기존: 기지 고정·가속 없음·플레이 이벤트 없음. 할인은 기본 에너지에만.
  → 변경: playCardByEffect({discountE:5}) — playCardFromHand에 opts.discountE 추가(가속 +1 후 총액에서 차감). 위치 선택·가속·플레이 이벤트 정상. 미구현: #6887 '증원 자체를 첫 카드로 세고 공개 유닛이 두 번째'는 주문을 해결 완료 시점에 세는 현행(351.3·#5061)과 충돌해 그대로 둠 — 증원 해결 중 나온 유닛의 seq는 증원을 포함하지 않는다.
  _근거: 353 (추가 비용 적용 후 할인 — 할인은 가속 에너지에도, #5164) · 352.4.a (통제 전장 선택) · 351.3_
### #94 스프라이트 부름
- **수정** — 기존: 파서가 위치 지정 없는 'Play a ... token'을 where:'base'로 컴파일해 손패에서 내면 무조건 기지 생성(숨김에서만 그 전장).
  → 변경: 위치 구절이 없으면 where:'play' → token op의 기지/통제 전장 선택 분기. 방어 중인 결전 전장에 바로 소환 가능. 프롬프트를 '토큰을 배치할 위치'로 바꿔 봇 배치 정책(POLICY.option /배치/)이 답한다. 영향 카드는 94 하나(다른 토큰 카드는 모두 here/base 명시).
  _근거: 179.1.a (토큰은 카드 플레이의 모든 단계를 따라 플레이된다) · 352.4.a (기본 유효 위치 = 기지 또는 통제 전장)_
### #100 보석세공 예언자
- **수정** — 기존: 감사 시점엔 token op·vanguardTokens가 visionCheck를 호출하지 않았다.
  → 변경: 배치1의 tokenPlayed()(effKw(u).vision → visionCheck, 토큰마다)가 token op·vanguardTokens 양쪽에 이미 들어가 있음을 확인 — 이번엔 회귀 테스트만 추가(선봉대 소집 4기 → [통찰] 4회).
  _근거: 351.3 (토큰은 카드가 아니지만 플레이될 수 있다) · 743.2 ([통찰]은 플레이될 때 격발)_
### #102 차원문 구출
- **수정** — 기존: makeUnit으로 직접 재소환 → 항상 탈진 등장, [가속]·entersReady(워윅)·enterReadyAura 미적용.
  → 변경: 추방 후 playCardByEffect(owner, n, {ignoreEnergy, ignorePower, playLoc:'base'}) → 정식 플레이 경로. 워윅 준비 등장, [가속] 선택 프롬프트, 추가 비용(잔혹한 후원자) 적용.
  _근거: 353 3단계 ("ignoring its cost"는 기본 비용만 0으로 — 군단 후위병 예시: 가속 추가 비용은 지불 가능) · 731 ([가속]은 선택 추가 비용)_
- **수정** — 기존: u.ctrl(통제자) 기지에 통제자가 재소환 — 빙의로 뺏은 유닛도 뺏은 쪽에 남았다.
  → 변경: u.owner가 자기 기지에 플레이(프롬프트·플레이 이벤트도 소유자 기준). 뺏은 유닛은 원 소유자에게 돌아간다.
  _근거: 카드 에라타 원문 "its owner plays it to its base" (RiftJudge #8127) · 107 소유자 구역_
- **수정** — 기존: 후보에서 토큰 제외(!u.isToken).
  → 변경: 토큰도 고를 수 있고, 추방되는 순간 소멸(재소환 없음, 플레이 이벤트 없음).
  _근거: 182 (토큰은 보드 밖 구역으로 가면 즉시 소멸) · 352.8 (대상 선택)_
- **수정** — 기존: runTriggerList(onPlay)만 → onYouPlayCard/Unit/OppTurn 미발화, playedCards 미증가.
  → 변경: 정식 경로에서 모두 발화(소유자 기준). 테스트: 시트리아 버프·playedCards 증가.
  _근거: 351.3 (효과로 낸 카드도 '플레이') · 빅토르 117 원문 "When you play a card on an opponent's turn"_
### #115 유망한 미래
- **수정** — 기존: makeUnit/execOps/gear.push 직접 처리 — 힘·추가 비용·[가속] 전혀 지불하지 않음.
  → 변경: playCardByEffect(pi, n, {ignoreEnergy:true}) → 힘 비용·[가속]·추가 비용을 정식으로 지불. 힘을 낼 수 없는 카드(주문은 대상 없는 것도)는 후보에서 제외(#3727·#5662), 없으면 5장 모두 재활용.
  _근거: 카드 원문 "ignoring Energy costs. (They must still pay Power costs.)" · 353 3단계 (ignoring its Energy cost → 에너지만 0, 나머지 비용 유지)_
- **수정** — 기존: placeUnit(u,'base') 고정.
  → 변경: 정식 경로의 pickUnitPlayLocation — 통제 전장, 죽음꽃 포식자의 적 전장 등 선택 가능.
  _근거: 352.3~352.5 (유닛은 플레이 시 유효 위치 선택: 기지·통제 전장, 카드 효과가 넓힐 수 있음)_
- **수정** — 기존: 주문을 execOps로 직접 실행 → resolveSpellEffects/fireSpellPlayEvents 미경유, 럭스 전설 등 '주문 플레이' 트리거 미발화. 유닛·도구도 onYouPlayCard/Unit 미발화.
  → 변경: 주문·유닛·도구 모두 playCardFromHand 경유 → 플레이 이벤트·추방 플래그(시간 왜곡)·등장 격발 정상. byEffect 주문은 응수 창(counterWindow)을 열지 않는다.
  _근거: 351.3 (카드는 그 과정을 전부 마쳤을 때 플레이된 것) · 351 1단계 (다른 효과가 해결 중이면 그것을 마저 해결 — 효과로 낸 카드엔 응수 창 없음, #3955)_
- **수정** — 기존: G.extraTurnFor 단일 플래그 덮어쓰기 — 양측이 각각 시간 왜곡을 내면 마지막 값만 남고, 상대 턴에 해결된 내 시간 왜곡은 내 다음 정규 턴 뒤에 붙었다.
  → 변경: G.extraTurns 큐(최근 해결분이 앞, unshift) → finishEndTurn이 shift()로 '이 턴 직후'에 하나씩 소비. 양측 모두 추가 턴을 얻는다.
  _근거: 카드 원문 "Take a turn after this one" · 100 (가능한 만큼 실행) — 룰북에 추가 턴 조항은 없어 판정(#2248 삽입 모델)을 따름_
- **수정** — 기존: byEffect 경로가 playRestriction을 통째로 건너뛰어 noPlay도 무시.
  → 변경: playCardFromHand: byEffect라도 TF().noPlay[p]면 거부(타이밍 제한만 면제). 실패한 카드는 playCardByEffect가 추방 상태로 둔다.
  _근거: 브린히르 26 원문 "opponents can't play cards this turn" · 100 (불가능한 지시는 무시) · #6039 (추방한 채로 남는다)_
- **수정** — 기존: 각자 재활용 직후 onYouRecycle 발화 → 카르마 버프가 새 유닛 등장 전에 해결.
  → 변경: 재활용 대상 플레이어를 모아 두고 양측 플레이가 끝난 뒤 onYouRecycle 발화 → 새 유닛도 버프 후보.
  _근거: 376.4.b (조건 충족 시 격발 능력은 체인에 오른다 — 해결 중인 효과가 끝난 뒤) · #3955 (효과로 낸 카드는 원 주문 해결의 일부)_
### #122 시간 왜곡
- **수정** — 기존: 한 턴에 2장 해결 시 1턴만.
  → 변경: 큐로 2턴. 테스트: 2장 → endTurn 3회에 걸쳐 A·A·B.
  _근거: 카드 원문 "Take a turn after this one" — 두 장이면 두 번 (#3974) · 룰북 미규정이라 판정 삽입 모델(#2248)_
### #161 죽음꽃 포식자
- **수정** — 기존: 첫 오로라의 playCardFromHand 끝 cleanup이 즉시 startShowdown → 두 번째 오로라가 결전 중에 해결.
  → 변경: endTurn이 onEndTurn 격발 동안 G._holdShowdown=true → cleanup이 결전 개시를 보류, 격발이 모두 끝난 뒤 endTurn의 cleanup(p)에서 한 번 개시. 테스트: 두 오로라 모두 중립 상태에서 배치 → 포식자 2기가 적 전장에 → 결전.
  _근거: 342.1.b (결전은 중립 열린 상태에서만 시작) · 333.1 (체인이 있으면 닫힌 상태) · 클린업 9단계_
### #183 조작된 덱
- **수정** — 기존: fx.nocturne 플래그를 아무 데서도 읽지 않아 대체 플레이가 전혀 없었다.
  → 변경: engine nocturneOffer(p, seen) 헬퍼 — 조작된 덱(lookTopHand)·[통찰](visionCheck)·증원/미끼 바늘(lookTopPlayUnit)·유망한 미래에서 본 녹턴마다 '추방하고 ✳ 지불 플레이' 확인 → payCost(['Any']) 후 playCardByEffect(비용 무시). 플레이한 녹턴은 손패 1장 선택에서 빠진다(#6744).
  _근거: 녹턴(194) 카드 원문 "덱 맨 위에서 나를 보거나 공개할 때, 나를 추방할 수 있다. 그렇게 했다면 ✳을 지불하고 나를 플레이할 수 있다" · 353 (지정 비용 ✳만 지불)_
### #242 미끼 바늘
- **수정** — 기존: makeUnit 직접 배치 → [가속] 불가, 항상 탈진.
  → 변경: lookTopPlayUnit이 추방 후 playCardByEffect({ignoreEnergy, ignorePower}) → [가속] 선택 가능.
  _근거: 353 3단계 (ignore는 기본 비용만; 가속 추가 비용은 선택 지불) · 731.6_
- **수정** — 기존: fx.addCost 무시 — 잔혹한 후원자를 공짜로, 지불 불가 시 추방 유지도 없음.
  → 변경: 정식 경로에서 killUnit 추가 비용 강제. 처치할 아군이 없으면 플레이 실패 → P.banish에 남음(#5989, playCardByEffect 공통 처리).
  _근거: 353.2 강제 추가 비용 ("As an additional cost"는 반드시 지불) · 353 (ignore는 기본 비용만)_
- **수정** — 기존: placeUnit(u,'base') 고정.
  → 변경: pickUnitPlayLocation — 방금 비워진 통제 전장도 후보(테스트: 마지막 유닛 처치 후 그 전장에 배치).
  _근거: 352.4.a (통제 전장은 유효 위치) · 클린업 전에는 빈 전장 통제가 유지됨(releaseEmptyBattlefields는 cleanup에서만)_
- **수정** — 기존: playedCards·onYouPlayCard/onYouPlayUnit 미발생 → 다리우스·[군단] 미반영.
  → 변경: 정식 경로가 playedCards++·플레이 이벤트를 낸다. 테스트: 첫 카드 후 미끼 바늘로 낸 다리우스가 준비된다.
  _근거: 351.3 (플레이) · 다리우스 27 원문 "When you play your second card in a turn"_

## 5차 — 개별 카드 #5~#80 (31건)
### #5 붕괴
- **수정** — 기존: ifItDead가 결전 역할 없는 might(it)로 판정 — 전투 중 [맹공]/[보호막]으로 살아남는 유닛에도 드로우
  → 변경: ifItDead가 클린업과 같은 잣대(targetMight: 결전 역할·forKill)로 판정 — 맹공 2 공격자(2+2)에 피해 3이면 생존·드로우 없음
  _근거: 룰 322 2b (클린업: 피해가 위력 이상인 유닛이 죽는다) · 704/727 결전 중 공/방 지정 위력 — RiftJudge #7148 "triggers if the unit dies during the cleanup immediately following the spell resolution"_
### #6 와작와작 죠스
- **수정** — 기존: jawsReplay가 makeUnit/placeUnit로 항상 기지에만 배치
  → 변경: 폐기장→손패 맨 앞→playCardFromHand(fromTrash, 비용 무시) 정식 경로 — 기지/통제 전장(자운 소굴·방어 중인 결전 전장 포함) 선택. 플레이 금지(noPlay) 중엔 제안하지 않고 실패 시 폐기장으로 복귀
  _근거: 룰 352.4.a "Valid locations include the controller's Base or a Battlefield the controller controls" — RiftJudge #5470 · #3111 · #10943_
- **수정** — 기존: rocketRecover(252)가 P.trash.push로 직접 버려 죠스의 onDiscardSelf가 실행되지 않음
  → 변경: discardFromHand 경유 — 죠스 격발(플레이) 후 로켓도 손패로 회수. 버림 이벤트(징크스 등)도 정상
  _근거: 카드 원문 "When you discard me, you may pay 분노 to play me" — RiftJudge #6209 "Flame Chompers being played instead of discarded doesn't prevent SMDR's retrieval"_
- **수정** — 기존: 직접 배치라 playedCards·onYouPlayUnit/onYouPlayCard 미발화, 태양 원반 등 등장 준비 미반영
  → 변경: #2와 같은 정식 경로 — playedCards 증가·플레이 이벤트(시트리아 버프 테스트)·등장 준비 오라 반영
  _근거: 룰 351.3 Playing Cards(플레이 = 정식 절차) · 406.4.a 효과로 플레이되는 카드 — RiftJudge #4866 "putting Chompers permanent on chain"_
- **수정** — 기존: activateAbility가 비용(버림→죠스 등장) 뒤에 armoryProtect 대상을 골라 막 등장한 죠스가 후보에 포함
  → 변경: activated 정의에 target spec(ab.target) — activateAbility가 비용 전에 대상을 고르고 ctx.pre로 op에 전달(결전 체인 항목도 pre 보관). 버려서 등장한 죠스는 후보가 아님
  _근거: 능력 플레이 절차 2단계 선택 → 4단계 비용 지불 (룰북 Activated Ability 절차) — RiftJudge #2485 "choose the target ... before Flame Chompers is in play"_
### #9 마법공학 광선
- **이미 해결** — 기존: (감사 시점) 후보 0·굴절 미지불이어도 주문 플레이·비용 지불
  → 변경: changelog-2 #16/#234(spellHasTargets·pickPreTarget·PRE_CANCEL)로 이미 해결 — 회귀 테스트(test-batch5 '규율/광선: 적법 대상 없으면 플레이 불가')로 확인
  _근거: 룰 352.8 (모든 대상에 적법 선택 필요) · 353.2.b.1 굴절은 강제 추가 비용 — RiftJudge #3502_
- **수정** — 기존: resolveSpellEffects가 G._casting=원 시전자로 고정 → 원 시전자의 nextSpellBonus가 탈취된 주문에 적용
  → 변경: G._casting=execAs(해결 시점 통제자) — 마도서 보너스·주문 처치 귀속 모두 탈취자 기준. 소모 플래그는 양쪽 해제
  _근거: 룰 "A spell is controlled by the player who played it"(155.2) · 탈취자가 플레이한 것으로 취급 — RiftJudge #1393 "they don't control Ravenborn Tome, the spell does regular damage"_
### #22 열 광선
- **수정** — 기존: killAllGear가 P.gear만 순회 — 유닛에 장착된 장비(u.gear)는 남음
  → 변경: 유닛의 u.gear도 비워 통제자 폐기장으로 (현재 카드 풀엔 equipCost 카드가 없어 실전 영향은 없음 — 테스트는 u.gear 직접 삽입)
  _근거: 카드 원문 "Kill all gear" · 룰 716 Attachment(장착된 카드도 도구) — RiftJudge #3302 "Attached equipment is still considered gear and would be destroyed"_
### #23 무허가 무기고
- **수정** — 기존: 아군 유닛이 없어도 버림·탈진 비용을 내고 발동(효과만 생략)
  → 변경: ab.target 후보가 0이면 발동 거부(비용 없음). 봇 polAbLegal도 같은 판정
  _근거: 룰 391.3 "If legal options are not available for an Activated Ability, it is not legal to activate it" — RiftJudge #3642_
- **수정** — 기존: u._armory 불리언 — 두 장을 걸어도 하나로 합쳐지고 회수 시 모두 소멸
  → 변경: u._armory 횟수 카운터(+1/−1) — 두 장 걸린 유닛이 죽으면 하나만 소모되고 1이 남는다
  _근거: 대체 효과는 각각 별개 — 하나가 사망을 대체하면 나머지는 대체할 사건이 없다 (룰 366~368 취지) — RiftJudge #686 "only one Unlicensed Armory is consumed"_
### #25 눈먼 분노
- **수정** — 기존: blindRage가 makeUnit(loc:'base')로 직접 배치, 플레이 이벤트 없음
  → 변경: 손패 임시 삽입 → playCardFromHand(byEffect, 비용 무시, owner:상대) 정식 경로 — 배치 위치 선택(통제 전장·결전 전장), 플레이 이벤트·playedCards. 소유자는 상대(makeUnit owner 전달), 다 쓴 주문은 소유자 폐기장
  _근거: 룰 352.3~4 유닛 배치 위치 선택 · 406.4.a 효과로 플레이 — RiftJudge #4963 "play it to your side of the battlefield ... You become the Controller"_
- **수정** — 기존: 추가 비용·[가속] 선택 없이 직접 배치/실행
  → 변경: 정식 경로라 [가속] 선택(군단 후위병 테스트)·강제 추가 비용 처리 — 낼 수 없으면 추방 상태로 남음
  _근거: 룰 353 3단계 "ignoring its cost ... base Energy cost and base Power cost are set to zero ... Further additional costs ... must be paid" — RiftJudge #2434_
### #26 브린히르 썬더송
- **수정** — 기존: blindRage 등 직접 배치 경로가 noPlay를 우회 (fromDeck/fromTrash 경로는 changelog-4 #256에서 이미 검사)
  → 변경: 눈먼 분노가 정식 경로(playCardFromHand: byEffect라도 TF().noPlay 검사)를 타서 금지 중엔 플레이 실패·추방 유지. 죠스 재플레이도 noPlay면 제안하지 않음
  _근거: 카드 원문 "opponents can't play cards this turn" · 룰 406.4 효과로 플레이해도 플레이 — RiftJudge #10521_
### #31 격노한 화염룡
- **수정** — 기존: nextSpellDisc를 resolveSpellEffects에서만 0으로 — 카운터된 주문은 할인이 남아 다음 주문에 재적용
  → 변경: playCardFromHand가 payCost 직후 주문의 nextSpellDisc를 0으로 소모(숨김·에너지 무시 플레이는 적용된 적이 없어 제외). 체인 해결 전에 이미 0
  _근거: 룰 351~353: 비용은 플레이 절차(확정 전)에서 산정·지불 · 408.2 카운터는 해결만 막음 — RiftJudge #4039 "consumed when you apply it during the process of playing a spell, even if that spell is later countered"_
### #32 갈까마귀 마도서
- **이미 해결** — 기존: (감사 시점) challenge가 kind 'spell'로 nextSpellBonus를 받음
  → 변경: changelog-3 #73(kind 'unit')로 이미 해결 — 코드 확인(challenge/fightMutual/gentlemenDuel 모두 'unit')
  _근거: 룰 405/406 피해의 출처(유닛이 서로 주는 피해는 주문 피해가 아님) — RiftJudge #7742_
### #43 매혹
- **수정** — 기존: SCRIPTS[158] cond가 src.loc==='base'일 때도 격발 — 기지의 볼리베어가 매혹 이동에 드로우
  → 변경: cond = 볼리베어가 전장에 있고(src.loc!=='base') 이동 목적지가 다른 전장일 때만
  _근거: 볼리베어(158) 카드 원문 "a battlefield other than mine (Bases are not battlefield)" — RiftJudge #8279 "Volibear must be on a battlefield (not in base)"_
- **수정** — 기존: chooseEffectMove의 UI.pickOption 취소(null)로 강제 이동을 거부할 수 있음
  → 변경: may가 없는 이동은 취소 시 '이동은 강제' 문구로 다시 묻고(최대 3회), 그래도 거부하면 첫 후보로 이동. optional 이동은 종전대로
  _근거: 룰 100 (할 수 있는 지시는 실행) · 425.3 Moving is a Limited Action — RiftJudge #9277 "you must execute the move if a valid destination exists"_
- **수정** — 기존: 중립 응수 창(reactionWindow) 후보가 손패 [반응] 카드·[반응] 능력뿐 — 전장에 숨겨 둔 카드로 응수 불가
  → 변경: reactionWindow가 상대의 숨김 카드(숨긴 턴·파괴공작원·737 대상·배치 불가 제외)를 후보에 넣고 playHidden(p, bf, 항목)으로 공개. 봇 정책은 이 후보를 쓰지 않음(기존과 동일)
  _근거: 룰 739.1 숨김 카드는 뒷면인 동안 [반응] · 309.2 닫힌 상태에서 [반응] 플레이 — RiftJudge #10372 "Tideturner must be revealed and played as a Reaction to the Charm spell while it is still on the chain"_
### #44 태엽 수호자
- **수정** — 기존: playCardFromHand 추가 비용 블록이 !opts.fromHidden으로 숨김 플레이를 통째로 건너뜀([가속]만 별도 허용)
  → 변경: 숨김 공개도 추가 비용(선택/강제)을 묻는다 — 숨긴 태엽 수호자를 공개하며 평정 힘을 내면 드로우
  _근거: 룰 353 3단계 "ignore ... set the appropriate Base Cost(s) to zero ... Further additional costs" · 738.1 숨김 공개는 기본 비용 0 — RiftJudge #2488_
### #45 저항
- **수정** — 기존: counterTargets가 x.p!==p로 자기 주문 제외
  → 변경: 결전 체인의 자기 주문도 후보(상대 주문 먼저·최신 순 정렬로 단일 자동 선택·봇 기본 선택은 상대 주문). 중립 응수 창은 상대에게만 열리므로 중립 상태의 자기 주문 카운터는 여전히 불가(희귀)
  _근거: 카드 원문 "Counter a spell"(진영 제한 없음) · 룰 352.9 자기 자신만 불가 — RiftJudge #8450 "Player A reacts with Defy, countering his own Stupefy"_
- **이미 해결** — 기존: (감사 시점) playedCards가 플레이 시점에 증가해 카운터돼도 세어짐
  → 변경: changelog-1 #8(주문 playedCards는 fireSpellPlayEvents에서만)로 이미 해결 — 회귀 테스트로 확인(카운터된 광선 후 playedCards 0)
  _근거: 룰 408.2 "the card wasn't played" · Counter "not considered to have been played" — RiftJudge #9459_
### #46 앙 가르드
- **이미 해결** — 기존: (감사 시점) resolveShowdown이 무혈 결전에도 전 유닛 치유
  → 변경: changelog-3 #76/#146(sd.hasCombat일 때만 치유)로 이미 해결 — 회귀 테스트로 확인
  _근거: 룰 142.4 치유는 턴 종료와 전투 정리에서만 · 437 무혈 결전은 전투가 아님 — RiftJudge #11147_
### #52 굳건한 포로
- **이미 해결** — 기존: (감사 시점) challenge가 역할 없는 might로 피해 계산
  → 변경: changelog-3 #74(targetMight)로 이미 해결 — 코드 확인
  _근거: 룰 727 [보호막] 방어자인 동안 — RiftJudge #575_
### #58 규율
- **이미 해결** — 기존: (감사 시점) 유닛이 없어도 규율을 내어 드로우만 가능
  → 변경: changelog-2 #16(spellHasTargets)로 이미 해결 — 회귀 테스트로 확인(play(58)===false)
  _근거: 룰 352.8 대상 지정 주문은 적법 대상 없이 체인에 오를 수 없다 — RiftJudge #8139_
### #64 바람 장막
- **부분 수정** — 기존: 자기 주문 카운터 불가
  → 변경: #25와 같은 counterTargets 수정으로 자기 주문 카운터 가능. #5306의 '바람 장막이 자기 자신을 대상' 부분은 룰 352.9와 상충해 구현하지 않음
  _근거: 카드 원문 "Counter a spell" · 룰 352.9 "A spell that says 'Counter a spell' cannot target itself" — RiftJudge #8450(자기 주문 카운터 가능)_
  _보류 사유: 자기 자신 대상(#5306)은 룰 352.9 명문과 상충 — 자기 주문 카운터만 반영_
### #69 최후의 저항
- **수정** — 기존: mightDouble이 역할 없는 might(u)를 스냅샷 — 쉔 전투 중 8, 전투 후 6
  → 변경: targetMight(u)(결전 역할 포함) 스냅샷 — 포로(2+보호막1) 전투 중 6, 전투 밖 5
  _근거: 카드 원문 "Double a friendly unit's Might this turn"(해결 시점의 현재 위력) · 727 [보호막] — RiftJudge #2568 "snapshots Shen's current Might (5) and doubles it to 10"_
### #70 마법사냥꾼 간수
- **이미 해결** — 기존: (감사 시점) onYouPlaySpell이 op 실행 전에 발화해 간수가 살아 있는 동안 준비 시도
  → 변경: changelog-1 #54(효과→클린업→fireSpellPlayEvents)로 이미 해결 — 회귀 테스트로 확인(간수 사망 후 다리우스 준비)
  _근거: 룰 407.3.a 플레이 격발은 해결 뒤 · 322 클린업 선행 — RiftJudge #7846_
- **수정** — 기존: playHidden이 fromHidden 유닛을 unitPlayLocationOptions 검사 없이 그 전장에 배치
  → 변경: playHidden이 유닛이면 unitPlayLocationOptions(p,n)에 그 전장이 있어야 공개 허용 — 간수가 전장에 있으면 거부(숨김 유지)
  _근거: 카드 원문 "opponents can only play units to their base" · 737.2 숨김 유닛은 그 전장에 등장 · 352.3 합법 위치 필요 — RiftJudge #4573 "no legal play location and therefore cannot be finalized"_
### #72 솔라리 성소
- **수정** — 기존: killUnit이 처치자를 G.actingPlayer로 추정 — 자기 칙령으로 죽은 자기 기절 유닛에 상대 성소 발동
  → 변경: 처치자 = 주문/칙령/단두대 시전자(killer), 없으면 상대(전투·능력). 처치자가 유닛 통제자 본인이면 미발화(적 유닛이 아님)
  _근거: 룰 416 처치는 지시한 효과의 통제자 · 카드 원문 "When you kill a stunned enemy unit" — RiftJudge #5587 "the player who cast Imperial Decree (you) gets credit for the kill"_
### #77 존야의 모래시계
- **이미 해결** — 기존: (감사 시점) 존야 대체 후 _decree가 지워져 한 번만 죽으려 함
  → 변경: changelog-3 #109(wasLethal&&hadDecree면 재처치)로 이미 해결 — 코드 확인
  _근거: 클린업 사망(322 2b)과 칙령 처치 격발은 별개 사건 — RiftJudge #8420_
### #80 신비한 반전
- **수정** — 기존: 탈취는 execAs만 바꾸고 원 시전자의 대상(item.pre)을 그대로 사용 — side 뒤집힘으로 사실상 불발
  → 변경: resolveSpellEffects가 탈취(execAs≠p) 시 탈취자 기준으로 preTargetSpell을 다시 돌려 새 대상 선택(같은 대상도 가능·byEffect라 후보 없으면 그 지시만 불발·굴절 면제 noDeflect·숨김 전장 제한 유지). 반사 격발 주문(reflexive)은 격발 시점 선택 그대로
  _근거: 카드 원문 "You may make new choices for it" · 룰 352.8 적법 대상이 있으면 골라야 함 — RiftJudge #4630 "You must choose legal targets if legal targets exist" · #3088 굴절 재지불 없음_

## 6차 — 개별 카드 #83~#206 (30건)
### #83 과거에게 묻다
- **수정** — 기존: 중립 상태 상대 주문의 응수 창(reactionWindow)에 손패 [반응] 카드·[반응] 능력만 나와 숨겨 둔 카드를 뒤집을 수 없었다
  → 변경: 응수 창에 '[숨김 공개]' 선택지 추가 — 내가 숨긴 카드(숨긴 턴·파괴공작원 봉쇄·737 대상 없음 제외)를 playHidden과 같은 fromHidden 정식 경로로 공개(비용 0, 주문은 즉시 해결+재응수 창). 온라인은 pickReaction 인덱스 락스텝 그대로
  _근거: 739.1 "A card that is Hidden gains Reaction while facedown" · RiftJudge #7188_
### #94 스프라이트 부름
- **수정** — 기존: 숨김에서 낸 스프라이트 부름의 token op가 간수(70) 검사 없이 그 전장에 토큰을 놓았다
  → 변경: token op: 전장 배치(_hiddenBf·here)에 상대 간수가 전장에 있으면 토큰만 불발(주문은 해결·폐기), 'play' 분기의 전장 후보도 제외
  _근거: 100 "do as much as you can, ignoring impossible instructions" · 마법사냥꾼 간수 원문 "opponents can only play units to their base" · #3977_
### #96 경계하는 보초
- **수정** — 기존: 이케시아 소나기의 6개 격발이 연속 실행돼 보초의 종소리 드로우가 전부 끝난 뒤에 났다
  → 변경: resolveSpellEffects가 반사 격발 주문(fx.reflexive, playOps 2개 이상)의 playOps 사이마다 cleanupDeaths — 죽은 보초의 종소리(절충 ①: 즉시 해결)가 남은 격발보다 먼저
  _근거: 322 클린업(격발 해결 사이) · 376.4.b 격발은 체인에 · #272 "Deathknell will be added to the existing chain after the first instance"_
### #104 후퇴
- **이미 해결** — 기존: fireSpellPlayEvents가 op 실행 전에 발화해 상대 턴에 후퇴 대상이 된 빅토르가 신병 토큰을 만들었다
  → 변경: 이미 해결 시점 발화 — test-batch6 '후퇴+빅토르' 회귀 테스트만 추가
  _근거: 407.3.a · 356.3.e.11 — changelog-1 #103(효과 실행·폐기 → cleanupDeaths → fireSpellPlayEvents)_
### #106 스프라이트 어머니
- **보류** — 기존: mightSetToOther가 현재 위력(레오나 -8·최소 1 적용 후)과의 차이를 tempM으로 더해 최종 1
  → 변경: 변경 없음
  _근거: 수렴 변이 원문 "increase its Might to the Might of another friendly unit" — 룰북에 '설정(set)' 수정치와 오라의 적용 순서 조항 없음_
  _보류 사유: 판정 #6980 자체가 '레오나 -8이 set 뒤에 마지막으로 적용된다'면서 결과를 3이라 해 내적으로 모순이고, 'CR 개정으로 명확화될 예정'이라 적혀 있음. 룰북(rules1.txt)에 위력 수정치 층 규칙이 없어 뒷받침되지 않음 → 절차 1에 따라 보류_
### #107 성취자 아바
- **수정** — 기존: avaHidden이 카드를 직접 배치/실행한 뒤 fireEvent('onPlayFromHidden')를 내 잉걸불 수도승(167)이 +2를 받고, 일반 플레이 이벤트는 빠졌다
  → 변경: playCardFromHand(byEffect, ignoreEnergy/Power, 유닛은 playLoc=아바의 전장+locByEffect) 정식 경로 — onPlayFromHidden 없음, 플레이 수·이벤트·등장 격발·[가속] 정상. 실패 시 손패에 남음. engine: opts.locByEffect(효과가 지정한 위치는 통제 검사 생략)
  _근거: 카드 원문 "play a card with [Hidden] from your hand here, ignoring its cost" · 353 "ignoring its cost" · #10475 · #1893_
### #112 카이사 - 진화자
- **수정** — 기존: playFromTrash 주문 경로가 execOps로 op만 직접 실행 → 시간 왜곡의 banishSelf 플래그가 무시돼 덱 밑으로 재활용
  → 변경: 주문도 폐기장→손패 맨 앞→playCardFromHand(fromTrash) 정식 경로(플레이 시점 대상·플레이 이벤트·추방 처리). thenRecycle은 해결 뒤 폐기장에 그 장이 돌아왔을 때만(사본 수 비교) 덱 밑으로
  _근거: 100 불가능한 지시 무시 · 카드 원문 "Then recycle it" · #4642 "Kai'Sa's recycle effect cannot find it anymore"_
### #122 시간 왜곡
- **수정** — 기존: 카이사로 폐기장에서 낸 시간 왜곡이 추방되지 않고 덱으로 재활용됨
  → 변경: #61과 같은 수정 — 추방 상태 유지, 추가 턴은 부여 (test-batch6)
  _근거: 카드 원문 "Banish this" · #4642_
### #123 억제되지 않은 힘
- **이미 해결** — 기존: 배치 순서상 첫 유닛이 자동 구원
  → 변경: 이미 통제자가 선택 (test-batch3에 검증 있음)
  _근거: 376.3 · #2420 — changelog-3 #77(killUnitsTogether가 존야 통제자에게 회수할 유닛을 먼저 묻는다)_
### #128 도전
- **이미 해결** — 기존: 플레이 격발이 도전 피해보다 먼저 나 다리우스가 7로 싸움
  → 변경: 이미 해결 뒤 격발 — test-batch6 '도전+다리우스: 적은 5 피해' 회귀 테스트 추가
  _근거: 356.3.e.11 · #5184 — changelog-1 #103 순서 변경_
### #129 맞대결
- **수정** — 기존: token op가 op.ready만 봐 맞대결을 쓴 턴의 신병·정령 토큰이 탈진 등장
  → 변경: token op: op.ready || TF().enterReady[p] || enterReadyAura || nextUnitReady(소비)로 준비 등장
  _근거: 351.3 "Tokens are not cards, but can still be Played" · 카드 원문 "Units you play this turn enter ready" · #8620_
### #150 크라켄 사냥꾼
- **수정** — 기존: 버프 할인(pips.pop)이 [가속] 힘 추가보다 먼저 적용돼 기본 힘 2에만 걸리고 가속 힘 1은 반드시 룬으로
  → 변경: pipDisc/discPips — 가속 결정 뒤 총 힘 비용에서 할인, 가속 가능 여부 검사도 할인 반영 (버프 3개 → 4E 0P 가속 등장)
  _근거: 353 3단계 "Determine Total Cost ... additional costs ... may raise the card's Total Cost" 뒤 할인 · #4836 · #7976_
### #156 파괴 공작
- **수정** — 기존: revealHandPick의 pickOption 취소를 허용해 손패만 보고 재활용을 안 시킬 수 있었다
  → 변경: 후보가 있으면 취소 불가(취소 시 첫 후보로 확정). 정신을 가르는 자(192)의 버림도 동일
  _근거: 카드 원문 "Choose a non-unit card from it, and recycle that card"(may 없음) · #4264_
### #160 눈부신 오로라
- **수정** — 기존: playCardFromHand가 false(강제 추가 비용 불가 등)면 기지에 강제 배치
  → 변경: playCardByEffect(ignoreEnergy/Power) — 플레이하지 못하면 추방 상태로 남음 (잔혹한 후원자 208·브린히르 금지). 위치 선택 취소도 강제 효과라 추방 유지
  _근거: 100 · 에라타(추방 후 플레이) · #6096 "The revealed unit remains banished" · #5205_
### #161 죽음꽃 포식자
- **수정** — 기존: teemoDefend가 pickUnitFrom으로 직접 골라 [굴절] 유닛을 대상으로 해도 힘을 내지 않았다
  → 변경: 대상 선택 뒤 payDeflect — 지불 거부 시 그 지시 소멸(기존 관례 #233)
  _근거: 735 굴절(대상 지정 시 추가 비용) · 353.2.b.1 "Deflect ... Mandatory Additional Cost" · #3529 · #3247(대상 하나라 1회)_
- **수정** — 기존: 종료 단계 결전(오로라→포식자)에서 정복해도 finishEndTurn이 readyRunesAtEnd를 읽어 룬 2개 준비
  → 변경: SCRIPTS[289] → EXTRA_OPS.targonConquer: G._endingTurn(종료 단계 진입 후)이면 플래그를 올리지 않음
  _근거: 317.1 Ending Step "At the end of the turn Game Effects take place"(한 시점) · 타곤의 정상 원문 · #5875_
### #164 세트 - 싸움꾼
- **수정** — 기존: 유닛 onConquer(세트 buffSelf) → 전장 onConquerHere(히라나 spendBuffThen) 순서라 방금 얻은 버프로 드로우
  → 변경: resolveShowdown이 정복 순간 버프 보유 유닛을 스냅샷(buffedAt)해 onConquerHere ctx로 넘기고 spendBuffThen이 그 유닛만 후보로
  _근거: 383.3.b / 룰북 "players may decline to pay for Triggered Abilities that have incurred a cost"(적재 시 지불) · #8738_
### #169 돌풍
- **이미 해결** — 기존: 카운터된 주문도 playedCards +1
  → 변경: 이미 fireSpellPlayEvents에서만 증가 — 카운터된 두 번째 카드 뒤의 카드가 seq 2
  _근거: 408.2 · #11080 — changelog-1 #12(주문의 playedCards는 해결 시점에만)_
### #171 신비한 포로
- **이미 해결** — 기존: [통찰]로 본 녹턴을 플레이 불가
  → 변경: 이미 구현 (test-batch4 '[통찰]로 본 녹턴도 플레이')
  _근거: 녹턴 원문 · #7709 — changelog-4 #183(nocturneOffer가 visionCheck에서도)_
### #172 질책
- **수정** — 기존: 손패 복귀(resolveReturnToHand)는 u.gear를 버려 장착 도구가 소실, killUnit은 통제자 폐기장으로 폐기
  → 변경: engine detachGear(u): 유닛이 보드를 떠날 때(사망·손패·차원문 추방) 장착 도구를 도구 통제자(equipGear가 기록한 u.gearCtrl)의 기지에 준비 상태로 회수
  _근거: 424.3·425 (Detach 위치) · 148 "Gear ... at a battlefield for any other reason, it is Recalled to its controller's base" · #1391 · #9348 · #677 · #43_
### #177 은밀한 추적자
- **수정** — 기존: 동행 확인이 표준 이동(moveUnits)에만 있어 바람 타기·매혹·아지르 등 효과 이동엔 따라갈 수 없었다
  → 변경: engine tagAlongFollow 헬퍼를 moveUnits·effectMove 양쪽에서 호출 — 동행 여부는 추적자 통제자가 확인, 동행 유닛도 fireAttackTriggers
  _근거: 카드 원문 "When a friendly unit moves from my location, I may be moved with it" · 427(효과 이동도 이동) · #6119 · #1531_
- **수정** — 기존: origins.filter(x!=='base')로 기지 출발을 제외해 기지의 탈진 추적자는 동행 불가
  → 변경: tagAlongFollow가 출발지가 기지여도 후보로 순회, 탈진 검사 없음
  _근거: 카드 원문 'my location'(기지 포함) · 비용 없는 능력 이동 · #2230_
### #180 희미해지는 기억
- **부분 수정** — 기존: 전장 유닛만 후보, 도구의 [일시적] 처치 로직 없음
  → 변경: 양측 기지의 미장착 도구도 후보(g.temporary) → startTurn 개시 절차에서 통제자의 [일시적] 도구를 killGear. 유닛 선택은 굴절 지불 유지
  _근거: 카드 원문 "Give a unit at a battlefield or a gear [Temporary]" · 742.1 "kill this"(영구물) · 720 장착 중 규칙 텍스트 비활성 · #836 · #6709_
  _보류 사유: 장착 중인 도구(u.gear 번호 배열)는 대상으로 고를 수 없음 — 판정상 장착 중엔 [일시적]이 격발하지 않고 분리돼야 처치되는데, 분리 뒤 표식을 잇는 구조가 없어 미구현_
### #181 경이의 꾸러미
- **수정** — 기존: wonderBundle이 P.gear만 순회해 장착 도구는 후보에 없었다
  → 변경: 아군 유닛의 u.gear 중 내가 통제하는(gearCtrl) 도구를 '장착 도구: …'로 추가, 선택 시 분리해 손패로
  _근거: 카드 원문 "Return another friendly gear ... to its owner's hand" · #10398 "An equipped gear is still a gear currently in play"_
### #187 회오리바람
- **수정** — 기존: 각자 자기 유닛만 후보(side:'friendly')
  → 변경: side:'any' + alreadyPicked(대상 지정이 아니라 굴절·대상 기록 없음) — 상대 유닛(적 바론 나쉬르)도 되돌릴 수 있음
  _근거: 카드 원문 "each player may return a unit to its owner's hand"('friendly' 없음) · #3563 · #9057 · #10445 (구 #1000·#548과 상충 → 최신 판정·원문 우선)_
### #198 괴롭히는 밤
- **수정** — 기존: playFromTrash 안에서 애니(310) onPlay가 즉시 돌아 아직 체인에 있는 괴롭히는 밤을 회수할 수 없었다
  → 변경: SCRIPTS[198] → EXTRA_OPS.harrowing: 유닛을 내기 전에 밤을 소유자 폐기장에 먼저 두고(G._spellPreTrashed) resolveSpellEffects가 이중 폐기를 건너뜀 — 애니가 밤을 회수 가능
  _근거: 351.3/407.3.a(주문은 해결을 마쳐야 플레이) · 376.4.b 격발은 해결 뒤 체인에 · #6899 "Spells enter the trash ... before Annie ... trigger goes on the chain"_
### #199 물결을 바꾸는 자
- **수정** — 기존: swap 후보가 '양쪽 모두 이동 가능'일 때만 나와 바일마우의 둥지(295) 유닛과는 교환 선택지 자체가 없었다
  → 변경: 교환을 두 독립 이동으로 — 한쪽만 가능해도 후보에 나오고, 가능한 쪽(물결을 바꾸는 자 자신)만 이동하는 부분 해결
  _근거: 100 · 356.3.e "Instructions that can be partially followed are followed as much as possible" · #7241 · #8972_
### #201 시간선 역전
- **수정** — 기존: [0,1] 고정 순서라 p1 시전 시 상대가 먼저 번아웃해 시전자가 이겼다
  → 변경: [ctx.p, opp] — 시전자(턴 플레이어)부터 버림·드로우 → 7:7·덱 3장 이하면 시전자가 진다
  _근거: 308 "If ... simultaneously, then Turn Order is referenced"(턴 플레이어부터) · #8457 · #3562_
### #203 빙의
- **수정** — 기존: killUnit이 trashCard(u.ctrl, ...)로 뺏은 쪽 폐기장에 넣어 재활용 가능
  → 변경: trashCard(u.owner ?? u.ctrl) — 원 소유자 폐기장 (종소리 격발자는 u.ctrl 유지). 장착 도구는 detachGear로 도구 통제자 기지로
  _근거: 룰북 폐기장 조항 "it's put into its owner's trash instead"(107 계열, 소유자 존) · #2250 · #4896_
### #206 등을 맞대고
- **수정** — 기존: 해결 시점에 최대 2기를 고르고 아군 1기여도 플레이 가능
  → 변경: PRE_TARGET_EXTRA.mightTwoDistinct(둘째 spec은 첫 대상 제외) — 플레이 시점 지정, 아군 2기 미만이면 spellHasTargets가 플레이 거부, 해결 때 한쪽 소실이면 남은 쪽만 +2
  _근거: 352.8(모든 필수 대상 지정) · 카드 원문 "Give two friendly units"('up to' 없음) · #1462 · #1242 · #3972_

## 7차 — 개별 카드 #207~#315 (30건)
### #207 영광의 부름
- **수정** — 기존: 중립 상태에서는 버프 소모 → 응수 창 → 해결 순이라 비용 지불과 해결 사이에 클린업이 없어 4피해 5⚔(버프 포함) 유닛이 버프를 소모해도 +3을 받고 살아남았다
  → 변경: playCardFromHand가 추가 비용 지불 직후(주문·중립 경로) cleanupDeaths()를 호출 — 버프를 잃어 치명이 된 유닛은 주문 해결 전에 죽고 +3은 대상 소실로 불발. 결전 적재 경로는 종전 cleanup(p) 유지
  _근거: 319.6 "After a Pending Item becomes a Legal Item on the Chain" 클린업 — 추가 비용을 낸 직후 치명 판정 (RiftJudge #10458 · #4473)_
### #212 미래의 용광로
- **수정** — 기존: killSelfGear 비용을 먼저 치른 뒤 해결 시점에 폐기장에서 고르므로 방금 폐기된 용광로 자신을 재활용할 수 있었다
  → 변경: activated[0].preTarget이 비용 지불 전에 '재활용할 카드 최대 4장'을 고정(ctx.preAb) — 자신은 아직 폐기장에 없어 후보에서 빠진다(같은 이름의 다른 사본은 허용). 해결 시 폐기장을 떠난 카드는 그 재활용만 소멸
  _근거: 352.8.a 선택은 비용(353~354) 전 — 대상 지정 시점엔 용광로가 아직 보드에 있다 (RiftJudge #11013 · #10944)_
- **수정** — 기존: 중립 상태 활성화 능력은 응수 창 없이 즉시 해결, 재활용 카드도 해결 시점 선택
  → 변경: activateAbility: preTarget이 있는 능력(212·242)은 비용 지불 후 reactionWindow(ctx.ability)를 열어 상대 [반응]에 응수 기회를 준 뒤 해결. 응수로 폐기장을 떠난 카드(불사조 재플레이)는 재활용되지 않는다. 결전 중 체인 항목도 preAb를 들고 간다
  _근거: 369 활성화 능력 "Opponents have an opportunity to respond, as appropriate, as if a card was played onto the chain" (RiftJudge #5445 · #10759)_
### #220 안면 분쇄
- **수정** — 기존: 플레이 시점 지정(2차 수정)은 됐으나 해결 시 아군이 죽으면 적의 적법성(facebreakerSpec '아군과 함께 있는 전장')이 깨져 적도 기절하지 않고, 적이 사라지면 `if(!en) return`으로 아군도 기절하지 않았다
  → 변경: facebreaker op: 플레이 시점에 쌍으로 고른 경우(preMode) 해결 때 각 대상의 적법성을 '전장의 적/아군'으로만 보고 남은 쪽을 기절시킨다
  _근거: 356.3.e "If an instruction has more than one Target and fewer than all of the Targets become Invalid... the instruction will execute, with only the Targets available" (RiftJudge #10273 최신 판정 우선, 구 #3021/#40과 상충)_
### #221 황제의 칙령
- **수정** — 기존: 기절 유닛 사망 시 killer를 G.actingPlayer 기준으로 정해 상대 턴 결전에서 내 칙령으로 내 기절 유닛이 죽으면 상대의 솔라리 성소가 격발했다
  → 변경: killUnit의 onYouKillStunned 귀속이 이미 계산된 killer(처치 지시 시전자·_spellDmgBy·칙령/단두대 시전자)를 우선 — 칙령 사망은 칙령 시전자의 처치
  _근거: 416 "A spell or ability that contains a Kill instruction is responsible for Killing the Unit" (RiftJudge #5587)_
### #231 레드로스 사령관
- **수정** — 기존: 고른 유닛 수만큼 힘 핍을 먼저 깎고 지불한 뒤 killUnit — 존야·무기고 대체로 살아난 유닛도 할인에 포함
  → 변경: killUnit이 실제 사망 여부를 돌려주고, 깎은 핍(discPips)을 기억해 살아난 수만큼 힘을 추가 지불(낼 수 없으면 로그만 남기고 할인 유지 — 355에 따라 플레이 자체는 유효)
  _근거: 415.5.c "It is only considered Killed if its origin was any zone on the board" + 카드 원문 "for each killed this way"; 355 대체된 비용도 지불로 본다 (RiftJudge #377)_
### #242 미끼 바늘
- **수정** — 기존: killUnit 반환값이 없어 존야·세트·최후의 전사로 살아나도 위력+1 기준으로 덱에서 유닛을 플레이했다
  → 변경: killUnit이 boolean(실제 사망) 반환 → luredHook은 살아났으면 maxM=-1로 5장을 보고 전부 재활용만 한다
  _근거: 415.5.c 보드에서 폐기장으로 간 것만 '처치' — 대체 효과가 처치를 대신하면 '처치된 유닛'이 없다 (RiftJudge #8203)_
- **수정** — 기존: 처치할 유닛을 해결 시점에 고르고 중립 상태에선 응수 창이 없어 상대가 대상을 제거해 불발시킬 수 없었다
  → 변경: activated[0].preTarget이 발동 시점(비용 전)에 처치할 아군을 고정(아군 없으면 발동 불가·비용 없음) → 비용 → reactionWindow → 해결. 대상이 사라졌으면 처치 없이 5장 확인·전부 재활용
  _근거: 352.8.a 대상은 발동 시 고정 · 369 상대 응수 기회 · 356.3.e 대상 소실 시 그 지시 불발 (RiftJudge #11249 · #11232)_
### #244 신성한 심판
- **수정** — 기존: 플레이어 [0,1] 고정 순서로 '자기' 유닛/도구/룬 2개만 남기고 초과분을 재활용 — 상대 것을 고를 수 없고 합집합 구조도 아니었다
  → 변경: divineJudgment 재작성: 턴 플레이어부터 보드 전체에서 유닛 2·도구 2·룬 2를 고르고(부족하면 상대 것 강제, 같은 대상 중복 선택 허용), 손패는 자기 것 2장. 두 사람의 선택 합집합만 남기고 나머지 재활용(토큰 소멸·룬은 룬 덱). 봇은 '남길' 문구를 이로운 선택으로 읽어 아군 우선
  _근거: 카드 원문 "Each player chooses 2 units, 2 gear, 2 runes, and 2 cards in their hands. Recycle the rest."(유닛·도구·룬엔 자기 것 제한 없음) · 308 동시 선택은 턴 순서 (RiftJudge #5977 · #3107 · #2155)_
### #248 이케시아 소나기
- **수정** — 기존: 6개의 damage op가 한 주문 해결 안에서 피해만 누적하고 치명 판정은 끝에 한 번 — 4⚔ 유닛에 6개를 몰아도 한 번만 죽고 존야가 살리면 남은 격발이 소진됐다
  → 변경: resolveSpellEffects가 fx.reflexive 주문의 playOps 그룹마다 cleanupDeaths()를 돈다 — 2발로 죽은 뒤 남은 격발은 다른 유닛으로, 존야가 회수해도 남은 격발로 다시 죽인다. 격발 사이의 [반응] 응수는 절충 ①(격발 즉시 해결) 그대로
  _근거: 383 'Do this N times' 반사 격발은 항목마다 별개의 체인 항목 · 319.7 "After a Chain Item is removed from the Chain" 클린업 (RiftJudge #6282)_
- **수정** — 기존: payDeflect가 spellOK 없이 payCost를 불러 카이사 전설(247)의 주문 전용 힘을 어떤 주문의 굴절에도 쓸 수 없었다
  → 변경: payDeflect(p,u,base): 플레이 시점(base 있음)의 굴절은 base.spellOK로 지불(전용 힘 사용 가능), 해결 시점(반사 격발·능력)의 굴절은 종전대로 전용 힘 불가
  _근거: 353.2.b.1 "The cost imposed by the Deflect keyword is a Mandatory Additional Cost"(주문 비용의 일부) — 반사 격발의 굴절은 주문 비용이 아니다 (RiftJudge #4616 · #7545)_
### #252 초강력 초토화 로켓!
- **수정** — 기존: fireEvent가 폐기장을 new Set()으로 훑어 같은 카드는 한 번만 격발, 정복 1회당 로켓 1장만 회수
  → 변경: 폐기장 격발(trashTrigger)은 장마다 따로(실행 전 스냅샷) — 3장이면 3회 묻고, 방금 회수한 로켓을 다음 격발의 버림 비용으로 쓸 수 있다
  _근거: 376.3 각 카드의 격발 능력은 조건이 충족될 때마다 따로 격발 — 폐기장의 로켓 3장은 3회 (RiftJudge #6974)_
### #256 여우불
- **수정** — 기존: foxfire가 UI.pickUnitFrom을 직접 써 payDeflect를 한 번도 부르지 않았다 — [굴절] 유닛을 공짜로 처치. noteSpellPick도 누락
  → 변경: 후보를 unitsBySpec으로 만들고 고른 유닛마다 noteSpellPick+payDeflect — 지불 불가 유닛은 후보 제외, 거부하면 선택 종료. 대상 선택 시점은 종전대로 해결 시(가변 수 대상 — 절충 ④)
  _근거: 735 [굴절] — 고른 유닛마다 추가 비용 (RiftJudge #7798 · #9310 '여우불은 대상을 고른다')_
- **수정** — 기존: 후보 필터가 u.loc!=='base'뿐이라 숨겨 둔 전장이 아닌 다른 전장의 유닛도 처치할 수 있었다
  → 변경: unitsBySpec 경유로 _hiddenBf 제한이 적용된다 — 숨김에서 내면 그 전장의 유닛만 후보
  _근거: 737 숨김에서 낸 카드가 고르는 대상은 그 전장 안에서만 (RiftJudge #7068)_
### #260 최후의 숨결
- **이미 해결** — 기존: 감사 당시엔 손패 주문에 '적법 대상 없으면 플레이 불가' 검사가 없었다
  → 변경: changelog-2 #16의 spellHasTargets + PRE_TARGET_OPS('ready'·'itDealsTo' 포함)로 이미 해결 — 전장에 적이 없으면 playRestriction이 거부(test-batch7 ⑪로 확인)
  _근거: 352.8 "valid choices must be made for all targets" (RiftJudge #5305)_
### #266 힘 착취
- **수정** — 기존: powerSiphon이 {v:-1,min:1}을 그대로 push하고 might()가 합산 뒤 clamp — 1⚔ 적이 뒤에 +1을 받아도 1
  → 변경: 적 유닛마다 현재 위력(targetMight) 기준 eff=max(1,cur-1)-cur를 snap으로 고정 — 1⚔ 적은 -0, 이후 +1이면 2
  _근거: 454.3.d.2 최소값 제한이 있는 감소는 적용 시점의 실효 감소분으로 스냅샷 (engine might op와 같은 규칙, RiftJudge #7780 · #2713)_
### #268 쌍권총 난사
- **수정** — 기존: n=0이면 전장 선택도 없이 종료, 피해도 dealDamage(u,n,'spell') 직접 호출이라 관문(effDmgBonus)·서적(n<=0 조기 반환) 보너스가 빠졌다
  → 변경: gunsBlazing: 0 지불도 진행하고 dealDamage(u, n+effDmgBonus)로; dealDamage는 서적(nextSpellBonus)을 더한 뒤 0 이하 판정 — 0 지불 + 관문 + 서적 = 2
  _근거: 카드 원문 "Pay any amount"(0 포함) · 공허의 관문 "Each instance of damage the spell deals is increased by 1" (RiftJudge #5791)_
### #279 요새화된 진지
- **수정** — 기존: chooseUnit spec이 side:'friendly'라 방어자의 자기 유닛만 선택 가능
  → 변경: side:'any'(+_prompt '[보호막 2]를 줄 유닛 선택' — chooseUnit이 spec._prompt를 쓰도록 해 봇은 '부여할' 문구로 아군 우선)
  _근거: 카드 원문 "choose a unit" — 진영 제한 없음 (RiftJudge #5211)_
### #282 히라나 수도원
- **수정** — 기존: 정복 시 유닛 onConquer(세트 버프) → 전장 onConquerHere 고정 순서, 버프 소모는 해결 시 확인 — 버프 없던 세트가 정복하면 새 버프로 드로우했고, 기존 버프를 먼저 쓰고 재버프하는 순서도 불가
  → 변경: resolveShowdown이 정복 시점 버프 보유(buffAtTrigger)를 스냅샷해 onConquerHere에 넘기고 spendBuffThen이 false면 비용 불가로 불발; 정복 격발이 2개 이상이면 통제자가 순서를 고른다(선택지는 전장 격발이 첫 항목 — 봇 기본값)
  _근거: '4. Pay Costs' — 비용이 있는 격발 능력은 체인에 올릴 때 지불(거절하면 체인에 오르지 않음) · 376.4.b.2.c 동시 격발 순서는 통제자 선택 (RiftJudge #8738 · #10196)_
### #285 약탈자의 거리
- **수정** — 기존: startShowdown이 bf.controller===defender일 때만 onDefendHere를 격발 — 무주공산 전장에 나중에 합류한 방어자는 쓸 수 없었다
  → 변경: 전투 결전이면 통제자 여부와 무관하게 방어자(경합을 먼저 걸지 않은 쪽)에게 onDefendHere 격발
  _근거: 전투 1단계 "The Defender is the player who did not apply the Contested status to the Battlefield" — 통제와 무관 (RiftJudge #4985)_
### #287 폭풍의 인장
- **수정** — 기존: 룬 색이 같으면 묻지 않고 마지막 룬을 자동 재활용(준비 룬이 사라질 수 있음), 재활용 전에 에너지를 띄울 수단 없음
  → 변경: recycleRune op: 항상 어느 룬을 돌릴지 묻고(준비/탈진 표시, 탈진 룬이 첫 선택지=봇 기본), 준비 룬을 돌리면 먼저 탈진해 에너지 1을 풀에 유지(손해 없는 선택이라 자동)
  _근거: 룬 탭은 응수 불가 [추가] 능력이라 격발 해결 전에 에너지를 풀에 띄울 수 있다 (RiftJudge #2810 · #5048)_
- **수정** — 기존: 정복 처리 순서가 유닛 → 전설/도구/폐기장 → 전장 → 전설 훅으로 고정
  → 변경: #270과 같은 수정 — 정복 격발 출처가 2개 이상이면 '정복 격발 해결 순서' 프롬프트로 통제자가 하나씩 고른다(키아나+인장 등)
  _근거: 376.4.b.2.c "the [controller of] the Abilities selects the order to place them on the Chain" (RiftJudge #8242)_
### #289 타곤의 정상
- **수정** — 기존: 종료 단계 중 정복해도 readyRunesAtEnd가 늘어 finishEndTurn이 룬 2개를 준비시켰다
  → 변경: SCRIPTS[289] onConquerHere에 cond:()=>!G._endingTurn — 종료 단계 진입 후 정복은 룬 준비 없음(득점은 그대로)
  _근거: 종료 격발은 종료 단계에 한 번만 — 그 뒤(오로라 격발로 열린 결전)의 정복은 '이 턴 종료 시' 조건이 이미 지났다 (RiftJudge #7424)_
### #292 꿈꾸는 나무
- **수정** — 기존: 떨어지는 별·이케시아 소나기의 해결 시점 선택도 _curKind==='spell'이라 noteSpellPick이 드로우시켰다(changelog-2 #120의 미수정 잔여)
  → 변경: resolveSpellEffects가 fx.reflexive 동안 G._reflexiveCast를 켜고 noteSpellPick이 이를 건너뛴다 — 반사 격발은 나무를 격발하지 않음
  _근거: 383 반사 격발은 별개의 격발 능력 — 주문이 아니라 격발이 대상을 고른다 (RiftJudge #386 · #7545)_
- **수정** — 기존: TF().bf292[p] 단일 플래그라 두 전장이 모두 꿈꾸는 나무여도 턴에 1장
  → 변경: bf292[p][bfIdx]로 전장별 — 각 전장에서 1장씩, 같은 전장 두 번째는 없음
  _근거: 카드 원문 "a friendly unit here ... for the first time each turn" — 전장(here)별 (RiftJudge #9241 'Per Location')_
### #295 바일마우의 둥지
- **수정** — 기존: tideTurner의 chooseEffectMove({swapUid})가 양방향 모두 가능한 짝만 후보로 내 둥지의 유닛↔기지의 물결을 바꾸는 자 조합이 아예 불가
  → 변경: chooseEffectMove/resolveEffectMove: 교환은 한쪽만 가능해도 후보이며 해결 시 가능한 이동만 실행 — 물결을 바꾸는 자는 둥지로, 둥지의 유닛은 제자리
  _근거: 356.3.e "Instructions that can't be followed... are ignored"(가능한 만큼만 실행) — 교환의 두 이동은 각각 판정 (RiftJudge #7241)_
### #296 공허의 관문
- **수정** — 기존: cardscripts의 직접 dealDamage 호출(티모 전략가 teemoDefend·dmgEqMyMight·갈취·tfGamble·우디르·신난다 dmgLastDiscardCost·쌍권총 난사)에 effDmgBonus가 빠졌다
  → 변경: 각 호출 지점에 effDmgBonus(u, ctx.p)를 더한다(유닛 주체 'unit' 피해·전투는 제외 그대로). dealDamage 중앙 처리는 damage/damageAll/dealSplit의 이중 가산 위험으로 보류
  _근거: 카드 원문 "Spells and abilities affecting units here each deal 1 Bonus Damage" (RiftJudge #3674 티모 한 인스턴스 +1 · #5791)_
### #311 점멸
- **수정** — 기존: moveSpec spec이 where:'bf'라 기지 유닛은 후보에서 제외
  → 변경: spec에서 where 제거 + stayOK — chooseEffectMove가 이미 기지에 있는 아군을 '이동 없음(대상 지정만)' 선택지로 제공, resolveEffectMove는 noteSpellPick만 하고 이동하지 않는다(봇은 이득이 없으면 고르지 않음)
  _근거: 352.7 이동 목적지 선택은 '목적지를 고르는' 주문만 — 점멸은 목적지가 기지로 정해져 있어 기지의 유닛도 적법 대상, 이동만 생략 (RiftJudge #10257 · 355.4)_
- **수정** — 기존: noteSpellPick의 플레이어당 단일 플래그
  → 변경: #274와 같은 수정(bf292[p][bfIdx]) — 두 전장이 모두 꿈꾸는 나무면 각 전장에서 1장, 같은 주문으로 같은 전장 두 유닛은 1장
  _근거: RiftJudge #9241 'Per Location' — 전장별 턴 1회_
### #315 선봉대 소집
- **수정** — 기존: 토큰마다 기지/통제 전장을 따로 물어 4기를 나눠 놓을 수 있었다
  → 변경: vanguardTokens가 위치를 한 번만 묻고('신병 토큰 4기를 배치할 위치 (모두 같은 곳)') 4기를 같은 곳에 플레이
  _근거: RiftJudge #2710 "All recruits ... must be placed in the same location" (카드 원문 "They can be played to your base or to battlefields you control" — 룰북과 상충 없음)_
